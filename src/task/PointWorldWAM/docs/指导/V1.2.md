建议直接做一个 **V1.2：Chunk WAM formulation fix**。不改任务定义，不碰 `Cm`，只修当前 V1.1 中 4 个明显问题，然后用 deterministic regression 判断架构上限。

核心目标：

[
\boxed{
\text{先证明 chunk architecture 本身能记住}
\rightarrow
\text{再恢复 Flow Matching}
\rightarrow
\text{最后恢复 forward/inverse/joint 联训}
}
]

---

# 1. V1.2 改动范围

建议新增配置，尽量复用现有代码：

```text
src/task/PointWorldWAM/
├── dataset_wam_chunk.py          # 修改 statistics
├── wam_v2.py                     # 修改 coordinate / skip / readout
├── train_wam_v2.py               # 保留 FM 主训练
├── train_wam_v2_regression.py    # 新增 deterministic upper bound
├── eval_wam_v2.py
│
├── modules/
│   ├── spatial_queries.py
│   ├── world_action_transformer.py
│   └── action_readout.py         # 新
│
├── configs/
│   ├── grab_wam_v2_chunk.yaml
│   └── grab_wam_v12_debug.yaml   # 新
│
└── research/
    └── wam_v12/
        ├── diag_regression.py
        └── visualize_regression.py
```

不要删除 V1.1 checkpoint / log。

---

# 2. P0：改成 per-timestep normalization

当前最大问题之一是 (k=1) 和 (k=10) 共用统计量。

改成：

[
\boxed{
\mu_W,\sigma_W\in\mathbb R^{K\times1\times3}
}
]

以及：

[
\boxed{
\mu_A^{L/R},\sigma_A^{L/R}
\in\mathbb R^{K\times30}
}
]

---

## World statistics

现在类似：

```python
world = item["world_chunk"].reshape(-1, 3)
```

改成按 timestep 聚合。

伪代码：

```python
world_sum = torch.zeros(K, 3, dtype=torch.float64)
world_sq_sum = torch.zeros(K, 3, dtype=torch.float64)
world_count = torch.zeros(K, dtype=torch.float64)

for item in dataset:
    # [K, N_obj, 3]
    world = item["world_chunk"].double()

    world_sum += world.sum(dim=1)
    world_sq_sum += world.square().sum(dim=1)
    world_count += world.shape[1]

mean = world_sum / world_count[:, None]
var = world_sq_sum / world_count[:, None] - mean.square()

world_mean = mean[:, None, :]          # [K,1,3]
world_std = var.sqrt()[:, None, :]     # [K,1,3]
```

normalize：

```python
(world - world_mean) / world_std
```

输入：

```text
[B,K,N,3]
```

可以直接 broadcast。

---

## Action statistics

不要：

```python
torch.cat(values, dim=0)
```

把 K 混掉。

改：

```python
# [num_samples, K, 30]
stacked = torch.stack(values, dim=0)

mean = stacked.mean(dim=0)     # [K,30]
std = stacked.std(dim=0)       # [K,30]
```

于是：

```text
left_mean  [K,30]
left_std   [K,30]
right_mean [K,30]
right_std  [K,30]
```

---

# 3. 加一个必须记录的 normalization sanity

跑一次 dataset diagnostic，输出：

```text
k=1:
  world displacement mean norm
  normalized RMS

k=5:
  ...

k=10:
  ...
```

我们要看到原始运动大致随 horizon 增长：

[
|\Phi_1|<|\Phi_5|<|\Phi_{10}|
]

但 normalize 后：

[
RMS(\bar\Phi_k)\approx O(1)
]

所有 (k) 量级接近。

Action 同样检查。

这是必要 sanity，不是消融。

---

# 4. P0：world noisy geometry 改成 noise-aware coordinate

当前：

```python
world_flow = denormalize_world(noisy_world)
candidate_object = object_points + world_flow
```

需要改。

---

## 第一版直接：

[
\boxed{
P_{\tau,k}
==========

P_t+
\tau_W,
\tilde\Phi_{\tau,k}
}
]

代码：

```python
world_flow = self.denormalize_world(noisy_world)

coord_scale = tau_world[:, None, None, None]

candidate_object = (
    object_points
    + coord_scale * world_flow
)
```

其中：

```text
tau_world: [B]
world_flow: [B,K,N,3]
```

---

### inverse

[
\tau_W=1
]

同时：

[
\tilde\Phi=\Phi^*
]

所以：

[
P_{\tau}=P_t+\Phi^*
]

完全保留 desired future geometry。

---

### forward 初始噪声

[
\tau_W=0
]

得到：

[
P_\tau=P_t.
]

因此不会让 iid Gaussian flow 把物体点云炸散。

---

### 中间状态

[
0<\tau_W<1
]

逐渐允许 candidate future geometry 进入 topology。

---

# 5. 但 noisy world flow 仍完整放在 feature

不要丢掉 FM state。

仍然：

[
f_i^{obj}
=========

[
P_t,
n_t,
\Delta P_{t-1},
\tilde\Phi_{\tau}
].
]

即：

```python
object_features = self.object_adapter(
    torch.cat(
        [
            object_points,
            object_normals,
            previous_flow,
            world_flow,
        ],
        dim=-1,
    )
)
```

所以区分：

```text
coordinate
= 可信度加权后的 candidate geometry

feature
= 完整 noisy FM state
```

---

# 6. Hand candidate coordinate 不改

这一部分当前思路是对的：

[
A_\tau
\xrightarrow{MANO}
H_\tau
]

然后：

[
\boxed{coord_{hand}=H_\tau}
]

继续保留。

因为即便 action noisy：

[
H_\tau
]

仍然至少处在 MANO manifold（MANO 可表示的合法手形空间）上，不是独立点噪声。

---

# 7. P1：把 PointWorld-style object skip 补回来

当前：

```python
world_velocity = self.world_head(
    spatial_outputs["object"]
    + world_context
)
```

改为：

[
\boxed{
F^{world}_{i,k}
===============

F^{PTv3}*{i,k}
+
F^{adapter}*{i,k}
+
C^{world}_k
}
]

代码：

```python
world_features = (
    spatial_outputs["object"]
    + object_features
    + world_context
)

world_velocity = self.world_head(world_features)
```

即恢复：

```text
local PTv3 interaction
+
raw/current point feature skip
+
temporal global context
```

这和 PointWorld 的思路更一致。

---

# 8. P1：action 不再 `mean(8 queries)`

当前：

```python
temporal[side].mean(dim=2)
```

删掉。

不要让：

[
8\ queries\rightarrow mean\rightarrow1
]

重新抹掉 specialization。

---

# 9. 新增 `ActionReadoutQuery`

我建议不是简单 flatten，而是再加一个很轻的 learned query。

每个 timestep 已经有：

```text
8 object tokens
8 left tokens
8 right tokens
```

共 24 个 temporal tokens。

给左手一个：

[
Q_A^L
]

右手一个：

[
Q_A^R.
]

分别 cross-attend：

[
[
Z_k^{obj},
Z_k^L,
Z_k^R
].
]

---

## `modules/action_readout.py`

可以这样：

```python
class ActionReadout(nn.Module):
    def __init__(self, dim: int, heads: int):
        super().__init__()

        self.query = nn.Parameter(
            torch.randn(1, 1, dim) * 0.02
        )

        self.q_norm = nn.LayerNorm(dim)
        self.kv_norm = nn.LayerNorm(dim)

        self.attn = nn.MultiheadAttention(
            dim,
            heads,
            batch_first=True,
        )

        self.mlp = nn.Sequential(
            nn.LayerNorm(dim),
            nn.Linear(dim, 4 * dim),
            nn.GELU(),
            nn.Linear(4 * dim, dim),
        )

    def forward(self, tokens):
        # tokens: [B,K,Q,D]

        B, K, Q, D = tokens.shape

        kv = tokens.reshape(B * K, Q, D)

        query = self.query.expand(
            B * K, -1, -1
        )

        update, _ = self.attn(
            self.q_norm(query),
            self.kv_norm(kv),
            self.kv_norm(kv),
            need_weights=False,
        )

        output = query + update
        output = output + self.mlp(output)

        return output[:, 0].reshape(B, K, D)
```

---

# 10. 左右手 readout 都看全部 World-Action tokens

在 `wam_v2.py`：

```python
all_temporal = torch.cat(
    [
        temporal["object"],
        temporal["left"],
        temporal["right"],
    ],
    dim=2,
)
```

然后：

```python
left_context = self.left_action_readout(all_temporal)
right_context = self.right_action_readout(all_temporal)
```

这样左手生成直接看到：

[
Object+Left+Right
]

整个 interaction。

---

# 11. Action head 后半段继续保留当前信息

最终：

[
[
C_k^{action,L},
E(\theta_t^L),
E(A_{\tau,k}^L),
E(\tau_A),
E(k)
]
\rightarrow v_{A,k}^L.
]

即替换：

```python
temporal[side].mean(2)
```

为：

```python
action_context[side]
```

其他不变。

因此原来：

```text
256 + 4×128
```

维度完全不需要改。

---

# 12. 先不要直接重新跑 Joint FM

这里我建议先加一个 deterministic upper-bound script。

原因是我们现在必须区分：

```text
A. architecture 根本不会 inverse
B. Flow Matching 难训
C. forward / inverse 多任务竞争
D. Euler rollout 出问题
```

---

# 13. 新增 `train_wam_v2_regression.py`

做两个完全独立实验：

```text
inverse_regression
forward_regression
```

不要混合训练。

---

# 14. Experiment A：Inverse deterministic regression

任务：

[
\boxed{
(S_t,\Phi^{obj,*}*{1:K})
\rightarrow
A^{GT}*{1:K}
}
]

---

## 输入 world

直接 clean：

[
\tau_W=1
]

[
world=\Phi^{GT}.
]

所以 object coordinate：

[
P_t+\Phi^{GT}.
]

---

## Action candidate

不要 Gaussian。

直接使用 **zero relative action**：

[
A^{candidate}=0.
]

对应：

[
H^{candidate}_k=H_t.
]

也就是：

```text
desired future object trajectory
+
current hand trajectory repeated K times
```

模型的任务非常明确：

> 根据未来 object effect，直接回归应该产生什么 MANO chunk。

---

## 注意 normalization

zero action 必须先转 normalized space：

```python
zero_action_metric = torch.zeros_like(
    batch["left_action_chunk"]
)

zero_action_norm = model.normalize_action(
    "left",
    zero_action_metric,
)
```

不能直接：

```python
torch.zeros_like(target_normalized)
```

因为 mean 不一定为零。

---

## loss

直接：

[
L_{action-reg}
==============

|\hat A-A^{GT}|^2.
]

加：

[
L_{surface}.
]

不要 FM。

即：

[
\boxed{
L=
L_{action-reg}
+
\lambda_sL_{surface}
}
]

---

# 15. Inverse regression 的判断标准

在全部 128 debug windows 上：

当前 identity：

[
44.57\text{ mm}.
]

目标不是刚刚低一点。

我建议 debug gate：

[
\boxed{
E_{surface}<30\text{ mm}
}
]

至少明显：

[
30\ll44.57.
]

如果甚至能过拟合到：

[
10\sim20\text{ mm}
]

就说明：

> architecture 有容量，主要问题在 FM / noise / multi-task。

如果 deterministic 都：

[

> 44.57mm
> ]

那就不应该继续折腾 Flow Matching。

---

# 16. Experiment B：Forward deterministic regression

任务：

[
\boxed{
(S_t,A^{GT}*{1:K})
\rightarrow
\Phi^{obj}*{1:K}
}
]

---

## Hand

clean GT action：

[
A=A^{GT}.
]

所以：

[
H_k=MANO(A_k^{GT}).
]

---

## Object candidate

使用 zero flow：

[
\Phi^{candidate}=0.
]

所以 coordinate：

[
P_k^{candidate}=P_t.
]

---

## 输出

直接回归：

[
\hat\Phi^{obj}_{1:K}
]

而不是 FM velocity。

loss：

[
L=
|\hat\Phi-\Phi^{GT}|^2.
]

---

# 17. Forward deterministic gate

当前：

[
zero-flow=40.60\text{ mm}.
]

clean action FM：

[
47.96\text{ mm}.
]

deterministic regression 至少应该：

[
\boxed{
E_{forward}<30\text{ mm}
}
]

而且 zero-action 条件显著变坏。

也就是：

[
E_{GT-action}
\ll
E_{zero-action}.
]

如果 direct regression 都过不了 zero-flow：

[
40.60mm
]

说明 forward spatial architecture 本身还没跑通。

---

# 18. Regression 不建议新建完全另一套网络

为了真正测试当前架构，**backbone / spatial query / temporal transformer 全部复用**。

只新增一个参数：

```python
prediction_mode:
    fm
    regression
```

或者 trainer 层直接解释现有输出。

最简单：

### inverse regression

把：

```python
left_action_velocity
```

直接视为：

```python
left_action_prediction
```

监督：

```python
target["left"]
```

不用新 head。

### forward regression

把：

```python
world_velocity
```

直接视为：

```python
world_prediction
```

监督：

```python
target["world"]
```

这样测试的就是**同一套 capacity**。

---

# 19. V1.2 训练顺序

我建议严格这样：

```text
Step 1
per-time normalization
        ↓
diag statistics

Step 2
noise-aware world coordinate
        ↓

Step 3
object skip
        ↓

Step 4
action readout query
        ↓

Step 5
inverse-only deterministic regression
128 windows
        ↓

Step 6
forward-only deterministic regression
128 windows
        ↓

只有两个都通过
        ↓

Step 7
inverse-only Flow Matching
        ↓

Step 8
forward-only Flow Matching
        ↓

两者分别通过
        ↓

Step 9
恢复 0.4 / 0.4 / 0.2 joint training
        ↓

Step 10
full 6868 windows
```

---

# 20. 不要一开始就再调 FM step 数

当前 Euler 20 step。

先不要：

```text
20
50
100
Heun
midpoint
```

乱试。

因为 deterministic 都没确认。

只要 regression 通过之后，才开始判断：

[
\boxed{
\text{FM training error}
\quad vs\quad
\text{Euler integration error}
}
]

---

# 21. Regression 通过后，FM 按这个顺序恢复

首先只 inverse：

```yaml
inverse_prob: 1.0
forward_prob: 0.0
joint_prob: 0.0
```

至少跑到：

[
E_{inverse}<identity.
]

再只 forward：

```yaml
inverse_prob: 0.0
forward_prob: 1.0
joint_prob: 0.0
```

至少：

[
E_{forward}<zero-flow.
]

最后才：

```yaml
inverse_prob: 0.4
forward_prob: 0.4
joint_prob: 0.2
```

这样才能判断是不是：

[
\boxed{\text{multi-task interference}}
]

也就是“多个任务共享参数后互相干扰”。

---

# 22. surface loss 暂时不用重新设计

现在：

```yaml
surface_weight: 10.0
```

先保留。

但第一次训练必须记录：

```text
action_loss raw
surface_loss raw
10 × surface_loss
world_loss
```

只检查是否有明显量级问题。

例如如果：

```text
action loss = 0.5
10*surface = 20
```

那就显然不合理，需要调。

不要提前做一堆 lambda sweep。

---

# 23. 这轮暂时不要动 Cm

`Cm` 的问题和 V1.2 是另外一层：

[
HandFlow\rightarrow ObjectFlow
]

存在明显 shortcut。

当前 V1.2 的目标是先回答：

[
\boxed{
\text{Explicit World Flow Chunk}
\leftrightarrow
\text{Bimanual MANO Chunk}
}
]

这个基础 joint WAM 到底能不能在 debug 上成立。

等它能成立以后，再考虑：

[
C^{int}
]

作为跨 embodiment interface。

否则同时修改 WAM 和 Cm，很难知道问题来自哪里。

---

# 24. 最终验收表

V1.2 第一阶段只看：

| 检查                               |        Gate |
| -------------------------------- | ----------: |
| MANO GT action → GT surface      |       ~0 mm |
| inverse deterministic            | **< 30 mm** |
| identity                         |    44.57 mm |
| forward deterministic            | **< 30 mm** |
| zero-flow                        |    40.60 mm |
| forward GT action vs zero action |     GT 显著更好 |
| 全部输出                             |      finite |

第二阶段 FM：

| 检查                |          Gate |
| ----------------- | ------------: |
| inverse FM        |    < identity |
| forward FM        |   < zero-flow |
| forward GT action | < zero-action |
| joint rollout     |        finite |

**这些没通过之前不要扩 6868 windows。**

---

## 可以直接给 Codex 的实现指令

```text
阅读当前 oyx 分支 src/task/PointWorldWAM，基于现有 V1.1 ChunkJointWAM 实现 V1.2。
不要重构无关代码，不删除 V1/V1.1，不修改 PointWorld submodule。

目标不是直接重新长训 joint FM，而是修复当前 formulation 并验证 deterministic upper bound。

必须修改：

1. dataset_wam_chunk.py
- world statistics 改为 per-physical-timestep：
  world_mean/world_std shape [K,1,3]
- left/right action mean/std 改为 [K,30]
- 不允许把 K 维 flatten 掉
- 增加 normalization diagnostic，确认各 horizon normalized RMS 同量级

2. wam_v2.py
- noisy world candidate coordinate 改为：
  candidate_object = object_points + tau_world * denormalized_noisy_world
  注意正确 broadcast 到 [B,K,N,3]
- world noisy flow 仍完整保留在 feature 中
- hand coordinate 继续使用 MANO(noisy action)，不要改回 current hand
- world head 补回 PointWorld-style skip：
  spatial_object + object_features + world_context

3. action readout
- 删除 temporal[side].mean(dim=2)
- 新增 modules/action_readout.py
- 每个 timestep 左/右各使用一个 learned action query
- query cross-attend 当前 timestep 的 object + left + right temporal tokens
- 输出 [B,K,temporal_dim]
- 再与 current MANO state embedding、noisy action embedding、tau embedding、physical-time embedding 拼接进入现有 30D head

4. per-subject MANO、canonical/finger/region、shared PTv3、WorldActionTransformer 全部保留。

5. 新增 train_wam_v2_regression.py
先做两个独立 deterministic upper-bound：

inverse regression:
- clean GT world chunk
- zero relative action 作为 candidate hand，注意 zero metric action 要先 normalize
- 不加 FM noise
- 直接把现有 action output 监督到 normalized GT action
- loss = action regression + surface loss

forward regression:
- clean GT action chunk
- zero metric world-flow 作为 candidate object，先 normalize
- 不加 FM noise
- 直接把现有 world output 监督到 normalized GT world chunk

两个实验都使用现有 128 debug windows，不扩 full dataset。

6. eval
记录：
- inverse generated MANO surface mm
- identity MANO surface mm
- forward world EPE mm
- zero-flow world EPE mm
- zero-action conditioned forward EPE mm

Gate：
- inverse deterministic < 30 mm，并显著优于 identity 44.57 mm
- forward deterministic < 30 mm，并显著优于 zero-flow 40.60 mm
- GT action forward 显著优于 zero-action
- 全部 finite

7. 只有 deterministic regression 通过后，才分别运行：
- inverse-only FM
- forward-only FM
最后才恢复 0.4 inverse / 0.4 forward / 0.2 joint。

不要做 broad ablation。
不要修改 Cm。
不要加入 rigid SE(3) output/head。
保持显式 object point-flow output。

把实验假设、修改、指标和结论追加到
src/task/PointWorldWAM/research/log.md。
```

这版的核心不是“再加网络”，而是先把当前 V1.1 中 **时间尺度归一化错误、world noise/topology 冲突、query 最终又被均值抹平**这几个问题清掉，再用 deterministic regression 把“架构能力”和“FM 难度”彻底分开。
