# ObjectInteractionCmv2 V1.14e.1：混合训练 checkpoint 有界恢复

- status: `FINAL`
- work_version: `V1.14`
- parent: [V1.14e](V1.14e.md)
- branch: `ai/cmresidual/v118c-v114a-integration`
- impact: `L2`
- approval: 用户于 2026-09-23 明确要求从失败 run 的 checkpoint 恢复后重试
- reuse: 同一分支、同一 MANO+Inspire 单轮训练和保护边界内的有界纠偏

## Goal / Change / Protected

为 V1.14a portable runner 增加显式 checkpoint 与源 run manifest 恢复；固定 checkpoint SHA、源 commit、
输入 SHA 和已完成 step，恢复 Adam 与抽样 RNG，并在 backward 后增加不改变梯度的 finite 门禁。保持数据、split、
每手 2048 点、GT、坐标、模型、loss、batch16、LR 0.001、seed42、总计 40024 step 和 validation 不变；不覆盖失败 run。

## Verification / Stop / Rollback

定向测试覆盖恢复身份拒绝、RNG 前移和 finite gradient；执行 `tools/verify.py --changed`，再以新 run_id 从显式
step-16400 checkpoint 恢复。checkpoint/输入不匹配、再次出现 non-finite 或服务异常即停止，不跳 batch、不裁剪梯度、
不降低 LR。回滚为停用恢复参数并回到提交 `d27750e`；原 run、checkpoint 和新 retry 产物全部保留。
