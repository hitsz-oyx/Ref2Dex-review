# ObjectInteractionCmv2 V1.14d：先行 GRAB/MANO 单组训练

- 状态：`FINAL`
- work_version：`V1.14`
- 基线：[V1.14c FINAL](V1.14c.md)
- branch：`ai/ObjectInteractionCmv2/v1.13-io-acceleration`
- 等级：`L2`
- approval：`user-approved`
- approval_basis：用户于 2026-09-22 明确要求在 Inspire cache 尚未完成时先用 MANO 手开始训练。

## 改变、保护与运行

改变：首轮 active groups 从 `grab/mano + grab/inspire_f1` 改为仅 `grab/mano`，权重为 1；使用已完成的
V1.14c MANO lean cache，在空闲 GPU6 上执行随机初始化 1 epoch。Inspire cache 在 GPU1 独立继续，不动态加入本 run。

保持：MANO cache、`1068/134/133` split、每手 2048 点、30 Hz、stride 1/2/3、对象采样、KNN32、2 cm、
GT、坐标/单位、模型、loss、batch16、LR 0.001 和 seed42 不变；不加载 checkpoint，不覆盖旧 run。

门禁：单组 split IDs/counts 必须与 MANO manifest 一致，配置和 1-step smoke 必须通过，正式 run 固定 clean
commit/config/input SHA。风险是单组结果不能解释为 MANO+Inspire 混合训练效果；回滚为停止独立 run 并保留 manifest，
不影响继续生成的 Inspire cache。
