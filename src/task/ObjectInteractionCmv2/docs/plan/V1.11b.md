# ObjectInteractionCmv2 V1.11b 执行计划：V1.9 口径的 V1.11 二域 validation 评估

- 状态：`FINAL`
- 关联基线：[V1.11 最终计划](V1.11.md)
- work_version：`V1.11.1`
- 操作类别：`code`、`experiment`、`operation`、`documentation`
- 影响等级：`L3`
- 审批：`user-approved`；用户要求按此前方案一致的二域混合评估评估当前结果。

## 范围

- 保留 V1.9 validation protocol：GRAB stride=1，ARCTIC 确定性 stride=5..10，逐 ARCTIC stride 报告 FlowMetrics，batch=64、GPU1、seed=42。
- 仅新增 V1.11 evaluation config 并令既有 runner 按 config schema 接受 V1.11.1 checkpoint；新 run manifest 使用 `work_version`。
- 读取当前 V1.11 `best.pt`，新建独立评估 output，不修改训练 checkpoint、cache、split、GT、模型或训练运行。

## 验证与回滚

- 验证 config/CLI、checkpoint architecture/version、每域 sample accounting、per-stride 合计与 manifest/metrics 输出。
- 非 finite、OOM、schema 或 checkpoint 不匹配时写 `FAILED`，不影响训练/cache。
- 反转本次 evaluator/config 提交并保留独立评估 output 即可回滚。
