# V1.11i：五组混合训练资源切换补充

- 状态：`FINAL`
- work_version：`V1.11.1`
- parent： [V1.11h](V1.11h.md)；base_commit：`b9acd32b0bc7aa7aca73e1a6b3f0a512cbf8dadb`；branch：`cmv2`。
- 等级：`L3` 运行资源合同补充；approval：`user-approved`，用户于 2026-09-20 要求先停止 GPU1+GPU3 run，改用物理 GPU0+GPU2。

## 仅改变

- 新配置为 `mixed_articulated_v1_11i_ddp.yaml`，显式记录 `physical_gpus=[0,2]` 和 `LimitNOFILE=262144`。
- 新 run 不复用已停止 run 的 service、进程或 output 写入；旧 run `cmv2_v111h_mixed_ddp_formal_20260920T111159Z_nofile262k` 保留为 `STOPPED` 证据。

## 保持不变

- 五组数据、固定 split/holdout、OakInk2 部件级语义、MANO:Inspire 与组内比例、stride `{1,2,3}`、每卡 batch64、global128、16 epochs、seed42、FP32、模型、loss、checkpoint 初始化和 validation 口径全部不变。
- 不停止或覆盖 GPU0/GPU2 上已有进程；启动前仍须满足每卡至少 20 GiB free memory，资源不足则停止并报告。

## 验证、回滚

- 在 GPU0+GPU2 重新执行真实数据 2-step smoke，核对前后向、15 组验证、checkpoint roundtrip、rank parity 和显存；通过后再启动新的正式 run。
- 回滚仅停止新 service，保留所有旧 run、cache、split 和 checkpoint；不改共享 `src/base` 或数据产物。
