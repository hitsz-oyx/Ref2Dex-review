# ObjectInteractionCmv2 V1.11 执行计划：OakInk2 MANO 全量导出与短 stride 二域训练

- 状态：`final`
- 对应指导：[指导/V1.11.md](../指导/V1.11.md)
- modification_version：`V1.11.1`
- 操作类别：`code`、`data`、`experiment`、`operation`、`documentation`
- 影响等级：`L3`
- 审批：`user-approved`；用户于 2026-09-19 明确要求立即启动 OakInk2 MANO 全量导出，并以 GRAB/ARCTIC stride `1..3` 并行训练。

## 并发运行

1. GPU2：复用 V1.4.5 已 smoke-through 的 `build_oakink2_mano_highres_v1_4`，读取完成态 OakInk2 selection、官方 annotation、Stage3 静态几何和 MANO asset；新 output root、独立 run ID，全部 1849 段。
2. GPU1：新增 V1.11 formal YAML，复用 `train_articulated_formal.py` 的随机初始化训练；最小扩展其 config whitelist，使其显式接受 V1.11 schema/version/stride，不影响 V1.7/V1.8 合同。

## 不变量

- 训练源 index/manifest、articulation metadata、模型、loss、KNN=32、batch=64、epoch=16、seed=42、checkpoint 写入协议均保持 V1.8；仅两域训练 stride 变为 `{1,2,3}`。
- 导出仍写 `4096` object pool、`3076` decoder compatibility 手流、`4096` MANO KNN 手流、KNN32 与 2 cm mask；所有数据写入唯一新 output root。
- 不修改旧 V1.7/V1.8/V1.9 配置或 run、现有 OakInk2 Inspire full cache、旧 stage4 queue、训练 cache/输入和 checkpoint。

## 验证与停止条件

- 启动前：GPU1/GPU2 均至少 20 GiB 空闲；四个导出输入和训练 index/manifest 存在；output destinations 不存在。
- 工程验证：`py_compile`、V1.11 config parser/load smoke、GPU 进程/manifest 进入 `RUNNING`；导出每段保持 `.partial` 原子提交，任何失败写 manifest 并终止为 `FAILED`。
- 训练检查点、`metrics.jsonl`、`train.log`、best/latest 均在独立 output；任一非 finite、OOM、schema 或写入错误标为 `FAILED`，不得覆盖旧 run。
- 回滚为停止各自 PID，保留其独立 output/manifest/log；删除 V1.11 config 与 whitelist 增量可回到 V1.10.1，不删除任何 cache target。
