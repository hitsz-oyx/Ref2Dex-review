# ObjectInteractionCmv2 V1.11c 执行计划：短 stride 分组 validation 评估

- 状态：`FINAL`
- 关联基线：[V1.11 最终计划](V1.11.md)
- work_version：`V1.11.1`
- 操作类别：`code`、`experiment`、`operation`、`documentation`
- 影响等级：`L3`
- 审批：`user-approved`；用户将评估 stride 明确为 1、2、3，并要求分开统计 EPE、预测模长和 GT 模长。

## 范围

- 新建 V1.11 short-stride evaluation config：GRAB/ARCTIC validation 均使用确定性 stride 集合 `{1,2,3}`、batch=64、GPU1、seed=42。
- evaluator 对两个域都按实际 stride 分组，输出 sample 数、flow EPE、预测流模长、GT 流模长与角度统计；保留旧 V1.9 结果字段兼容。
- 读取 V1.11 epoch-9 `best.pt`，写独立评估 output。

## 不变量与回滚

- 不改变正 stride transition 定义、模型、checkpoint、GT、坐标、单位、cache 或 split。
- 失败时仅使新评估 run 标记 `FAILED`；反转 evaluator/config 提交或删除独立 output 不影响训练与数据。
