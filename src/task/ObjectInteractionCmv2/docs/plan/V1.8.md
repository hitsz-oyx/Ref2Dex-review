# ObjectInteractionCmv2 V1.8 执行计划：B64 二域正式训练重启（final）

- 状态：`final`
- 对应指导：[指导/V1.8.md](../指导/V1.8.md)
- modification_version：`V1.8.1`
- 类别：`code`、`experiment`、`operation`、`documentation`
- 等级：`L3`
- 审批：`user-approved`；用户在明确说明“停止 B16、以 B64 从随机初始化重启、先做 1-step calibration”的方案后回复“可以”。

停止并封存 V1.7.1 run：保留其 output、config、manifest 与 latest checkpoint，manifest 记为 `STOPPED`，不得删除或将其 checkpoint 用作新 run 初始权重。新增 B64 calibration config/runner，读取相同的冻结 two-domain MANO index/cache 和 V1.5 articulation metadata，固定每个 batch 为 GRAB=32、ARCTIC=32，执行一步 finite forward/backward/optimizer，并把独立 manifest、metrics、train log 与 latest checkpoint 写入新的 calibration output。

若 calibration 成功，V1.7 formal runner 仅扩展为识别显式 V1.8 schema 与 `batch_size=64` 合同；新增 V1.8 正式 config，以 GPU1、随机初始化、16 epoch、GRAB stride=1、ARCTIC deterministic stride=5..10、validation stride=2、source-balanced sampling 启动新的 output。训练步数按新 batch 重算为 `ceil((GRAB rows + ARCTIC rows)/64)`；不改变总 epoch、数据语义或 selection metric。若 calibration/正式运行 OOM、non-finite、FK/schema error，停止且不覆盖 V1.7.1。

验证：`py_compile` 新/改 runner；Task targeted pytest；B64 calibration manifest 为 `COMPLETED` 且 metrics 记录 finite loss/source counts；启动后核验 V1.8 manifest、PID、GPU1 与 checkpoint。回滚：停止新 PID，将 manifest/activity 记为 `STOPPED`，保留两个独立 output；删除 V1.8 代码/config/docs 与恢复版本指针即可，不删除 cache、旧 run 或其他 GPU 任务。
