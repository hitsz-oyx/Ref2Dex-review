# ObjectInteractionCmv2 V1.7 执行计划：V1.5 从零正式二域训练（final）

- 状态：`final`
- 对应指导：[指导/V1.7.md](../指导/V1.7.md)
- modification_version：`V1.7.1`
- 类别：`code`、`experiment`、`operation`、`documentation`
- 等级：`L3`
- 审批：`user-approved`；用户明确指定 GRAB stride=1、ARCTIC stride=5..10 和 16 epoch，并延续已确认的 GPU1/batch=16/随机初始化范围。

新增 Task-local full runner/config。每个 epoch 的样本数为两域全部 valid train rows 之和；每个 even batch 固定 GRAB/ARCTIC 各半，奇数尾 batch 也保持 source-balanced replacement。GRAB train 只使用 stride 1，ARCTIC train 以 seed=42、sequence/current-frame 稳定哈希从 5..10 选一个 stride；validation 两域均为 stride 2。随机初始化，不加载任何 checkpoint；每 epoch 保存 latest、按 equal `val_grab_loss/val_arctic_loss` 选择 best。

输出写新 `outputs/ObjectInteractionCmv2/<run_id>/`，含 config、run manifest、metrics、train log、best/latest。输入 cache/index、split、V1.5 metadata、旧 output/checkpoint、GPU2 queue 均只读。任一 FK/schema/non-finite/OOM 停止并写 FAILED。工程可运行不构成科研效果结论；16 epoch 终态后再做独立评估。
