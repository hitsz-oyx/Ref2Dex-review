# ObjectInteractionCmv2 V1.9 执行计划：现有 validation split 二域评估（final）

- 状态：`final`
- 对应指导：[指导/V1.9.md](../指导/V1.9.md)
- modification_version：`V1.9.1`
- 类别：`code`、`experiment`、`operation`、`documentation`
- 等级：`L3`
- 审批：`user-approved`；在告知当前 test split 不含 ARCTIC 后，用户明确要求“先只做验证集测试”，并确认沿用 GRAB stride=1、ARCTIC stride=5..10。

新增 isolated validation-evaluation runner/config，读取冻结的 two-domain MANO index/cache、V1.5 articulation metadata 和 V1.8.1 `best.pt`（epoch 9）；不得加载/修改 latest checkpoint。GRAB validation 使用 stride=1；ARCTIC validation 按 seed=42、sequence/current frame 稳定映射到 5..10，输出 GRAB、每个 ARCTIC actual stride、ARCTIC pooled、all pooled 的 point-micro flow EPE、预测/GT magnitude 和有效流向角。推理 batch=64、GPU1，仅在空闲显存至少 20 GiB 时启动；训练 PID 不停止。

输出写新的评估 run，包含 config snapshot、manifest、metrics summary 与 eval log。任何 checkpoint/schema/non-finite/OOM 使评估标记 `FAILED`，不影响训练。该 run 是 validation diagnostic，不得称为 held-out test 或泛化结果。验证：py_compile、现有 FlowMetrics 定向测试、评估 manifest/metrics 的 sample accounting 和 per-stride 合计。回滚：停止评估 PID 并保留独立 output；删除 V1.9 runner/config/docs、恢复 pointer 即可，不删除 cache、checkpoint、训练 output 或改变 split。
