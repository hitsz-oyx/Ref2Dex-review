# ObjectInteractionCmv2 V1.6 执行计划：V1.5 从零双域校准（final）

- 状态：`final`
- 对应指导：[指导/V1.6.md](../指导/V1.6.md)
- modification_version：`V1.6.1`
- 类别：`code`、`experiment`、`operation`、`documentation`
- 等级：`L3`
- 审批：`user-approved`；用户确认 GPU1、递增 batch 校准和 8-step smoke 范围。

新增独立 calibration runner/config：从既有冻结 GRAB/ARCTIC MANO index 只读取各一条稳定 train sequence，按偶数 batch 内 `1:1` 交替采样，随机初始化 V1.5，不加载 checkpoint。依次尝试 batch `2,4,8,16`；OOM 时停止增大，选择最大成功 batch 后运行 8 optimizer steps。输出只写新 `outputs/ObjectInteractionCmv2/<run_id>/`，包含 config、manifest、metrics、train log、latest checkpoint。

保持 V1.5 FK metadata、V1.4 runtime/checkpoint、cache、split 和 queue 不变。任一 FK/schema/non-finite/OOM 以 FAILED 或受控 calibration 终态记录；不进入正式训练。验证包括 Task pytest、GPU1 run manifest 和 activity 链接审计。回滚为停止/忽略独立输出并删除本计划新增 Task-local runner/config；不删除输入或旧产物。
