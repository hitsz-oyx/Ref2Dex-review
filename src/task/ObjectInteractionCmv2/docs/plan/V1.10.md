# ObjectInteractionCmv2 V1.10 执行计划：数据与 cache 软链接导航层

- 状态：`final`
- 对应指导：[指导/V1.10.md](../指导/V1.10.md)
- modification_version：`V1.10.1`
- 操作类别：`governance`、`data`、`documentation`
- 影响等级：`L3`
- 审批：`user-approved`；用户于 2026-09-19 确认按所提布局执行。

## 范围

在既有的 `data/processed_data` NAS 软链接根下新建 `oicmv2/`，创建以下绝对目标软链接，并新增此计划、指导、目录 README 和必要活动记录：

```text
oicmv2/
├── data/
│   ├── grab/mano -> oicm_v1_4_raw/grab_mano_30hz
│   ├── grab/inspire_f1 -> oicm_v1_4_raw/grab_inspire_bilateral_v2
│   ├── arctic/mano -> oicm_v1_4_raw/arctic_mano_30hz
│   ├── arctic/inspire_f1 -> oicm_v1_4_raw/arctic_inspire_bilateral_v2
│   └── oakink2/selection -> oicm_v1_4_raw/oakink2_active_tool_segments_v1_1
└── cache/
    ├── mano/grab_arctic_split_seed42 -> oicm_v1_4_4/two_domain_mano_split_seed42
    ├── mano/grab_arctic_v1_4 -> object_interaction_cm_grab_arctic_mano_geometric_v1_4
    └── inspire_f1/grab_arctic_v1_4 -> object_interaction_cm_grab_arctic_inspire_geometric_v1_4
```

## 保持不变

- 所有现有 `data/processed_data/*` 路径与 NAS 实体保持原名、原 inode 和内容；不覆盖已有目录。
- 当前 V1.8/V1.9 GRAB+ARCTIC MANO 训练/评估仍使用既有 `oicm_v1_4_4/two_domain_mano_split_seed42` 绝对配置路径。
- OakInk2 Inspire `full_20260917T091507Z` 为失败后保留的 partial 输出，不能以 `cache/inspire_f1/oakink2` 或其他未限定名称建立入口。

## 验证与回滚

- 创建前解析每个 target，要求其为既有目录；创建后逐条以 `readlink -f` 比较规范绝对目标，并确认所有路径均在 `data/processed_data/oicmv2/` 下且均为软链接。
- 检查当前训练配置内容不变，运行 `git diff --check` 和 activity link audit。
- 回滚为只删除本次新增的 `oicmv2/` 目录树（其中仅含软链接和 README）以及本次 Task 文档/版本指针增量；绝不删除 link target 或其他 `processed_data` 内容。
