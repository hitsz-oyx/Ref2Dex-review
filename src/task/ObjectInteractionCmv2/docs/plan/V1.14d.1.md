# ObjectInteractionCmv2 V1.14d.1：本地 GRAB Lean Cache 物体 Pose 纠偏

- 状态：`FINAL`
- work_version：`V1.14`
- 父计划：[V1.14d FINAL](V1.14d.md)
- branch：`ai/ObjectInteractionCmv2/v1.13-io-acceleration`
- 等级：`L2`
- approval：`user-approved`
- approval_basis：V1.14d MANO-only 真实 1-step smoke 暴露本地 Stage4 缺失 pose；用户于 2026-09-22 批准继续修复。
- 复用分支理由：这是同一 MANO-only 训练、同一 GT/坐标保护边界内的有界输入纠偏，不引入新研究问题。

## 改变与保持

改变：对每条 lean sequence，以 frame0 的固定对应 4096 物体点为局部参考，通过刚体 Kabsch 恢复逐帧
`obj_pose_world`，原子替换错误的单位位姿并记录 provenance/replay 误差；重建引用更新后 manifest 的 active split。

保持：对象点/法向/ID、手点/法向、2 cm mask、frame ID/time、split、GT 定义、单位、模型、loss、训练预算和
checkpoint 解释不变。该修复恢复既定刚体 pose 合同，不改变物体轨迹或重新采样几何。

## 门禁、风险与回滚

每帧旋转必须为 SO(3)，全序列点 replay 最大误差必须不超过 `0.2 mm`；1335 条全部通过、manifest `bad_count=0`、
MANO 1-step optimizer smoke finite 后才启动 GPU6 正式训练。风险是点 correspondence 不固定或非刚体；任一失败即停止。
原 pose 文件先以同目录 `.v114d1_backup.npy` 保留，验证与训练启动后仍可逐序列原子回滚；不修改旧 NAS cache。
