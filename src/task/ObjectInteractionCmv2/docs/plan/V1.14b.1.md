# ObjectInteractionCmv2 V1.14b.1：隔离安装 OakInk2 MANO 运行依赖

- 状态：`FINAL`
- work_version：`V1.14`
- parent：[V1.14b FINAL](V1.14b.md)
- branch：`ai/ObjectInteractionCmv2/v1.13-io-acceleration`
- 等级：`L3`（本机 runtime dependency）
- approval：`user-approved`
- approval_basis：用户于 2026-09-22 明确允许 AI 自行安装缺失依赖。

## 范围与保护

在现有专用 Conda 环境 `graspenv` 中安装 OakInk 官方要求的固定 `manotorch v0.0.2`；该环境已具备 PyTorch 与
dex-retargeting，且不属于系统 Python。不修改仓库依赖合同、模型、数据语义或训练变量，不启动 full cache 或训练。

验证左右手 quaternion MANO 导入与本地 assets 加载，并重跑一条最多 8-frame OakInk2 Stage3 smoke；允许用
显式 sequence/frame offset 选择已知双手交互窗口，不改变数据筛选合同。记录 Python、PyTorch、manotorch 和
dex-retargeting 身份。若 Python/API 不兼容则停止，不升级现有 PyTorch 或其它大型依赖。

风险是 pip 传递依赖改变既有 runtime。安装前后保存 `pip freeze`；回滚仅卸载本次新增包，若发生依赖替换则按
安装前快照恢复。环境修改不提交 Git；长期事实写入 V1.14b Activity。
