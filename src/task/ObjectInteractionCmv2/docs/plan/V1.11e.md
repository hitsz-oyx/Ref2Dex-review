# ObjectInteractionCmv2 V1.11e 计划草案：Inspire 残留隔离与独立托管续跑

- 状态：`FINAL`
- work_version：`V1.11.1`
- 关联基线：[V1.11](V1.11.md) 与 [V1.11a](V1.11a.md)
- 影响等级：`L3`（现有 cache 路径改名与长时数据处理）
- approval：`user-approved`；用户于 `2026-09-20` 明确同意隔离空残留并使用独立服务继续启动。
- base_commit：`b9acd32b0bc7aa7aca73e1a6b3f0a512cbf8dadb`
- branch：`cmv2`

## 新发现与审批原因

V1.11a 保护项为“不覆盖或删除既有 geometry、`.partial`、旧 manifest、cache、checkpoint 或训练输出”。
本次预检发现下列上次中断残留；其中只有空 `geometry/` 子目录，无普通文件或软链接：

`/mnt/ugreen_nas/storage/Ref2Dex_storage/processed_data/oicm_v1_4_5_highres_full/grab_inspire_20260918T022000Z/sequences/train/inspire_f1/grab/s9/bowl_lift.partial`

现有 producer 在该路径已存在时抛出 `FileExistsError`，无法完成对应序列；不能通过直接启动规避。
保持原路径不动的选项是暂停该次全量恢复；拟议选项为原样改名保留这个空残留，不删除、不覆盖任何内容。
本补充只对上述一个已核验残留放行，不授权一般性清理、其他目录迁移或修改研究指导。

## 拟执行范围

1. 启动前再次确认没有同目标 producer、该残留仍只有空 `geometry/`，且同级隔离目标不存在。
   将其在同一 NAS 父目录原子改名为 `bowl_lift.partial.interrupted_<本次UTC时间戳>`，记录源、目标和目录清单。
   任何内容变化、额外残留或目标冲突均停止并报告，不自动删除。
2. 固定当前提交 `b9acd32b0bc7aa7aca73e1a6b3f0a512cbf8dadb` 作为新运行身份，记录脏工作树事实和相关源文件校验。
   上次续跑提交为 `f9f944ebc75efa3c5d16452809cfe348c7d7f737`；已核验两个提交之间及当前未提交修改中，
   `ObjectInteractionCmv2/tools/data`、`ObjectInteractionCm/tools/data`、`ObjectInteractionCm/research/hand_region_sampling`
   均无差异。保持 `work_version`、输入 index / source root、retargeting、GPU2、frame batch 2、object chunk 512 不变。
3. 新建独立运行身份和配置、manifest、日志目录：`data/processed_data/oicmv2_inspire_resume/v1/<run_id>/`，实体位于 NAS。
   保留原 queue 及其配置、状态、日志和历史 run manifest；不复用旧 run ID。
4. 用 `systemd --user` 服务托管，不依赖当前会话 shell；stdout / stderr、临时目录和退出状态写入上述 NAS 运行目录。
   保存 unit 名称、PID、命令、输入校验及服务退出结果；不启用自动无限重启、不修改系统服务或用户 linger 配置。
   当前 `Linger=no`，只能保证脱离当前 Codex 会话，不能承诺整机重启或用户会话管理器退出后继续运行。
5. 先以 `--resume` 验证并复用 GRAB 已完成的 972 条，再补其余 363 条。GRAB 全量成功后才按 V1.11a 串行启动
   ARCTIC 的 301 条；其输出根使用原队列预定且当前不存在的
   `/mnt/ugreen_nas/storage/Ref2Dex_storage/processed_data/oicm_v1_4_5_highres_full/arctic_inspire_20260918T022000Z`。
   已完成的 OakInk2 MANO / Inspire 不重导，不恢复旧队列中已被替代的 OakInk2 job。

## 保持项、风险与停止条件

- 不改变 GT、坐标、单位、split、点数、KNN32、schema、模型、训练或 checkpoint 解释；不覆盖 972 条已完成 geometry。
- 本地盘约剩 `2.9 GiB`、NAS 约剩 `51 TiB`；不自动清盘。临时目录和日志改放 NAS以减轻本地写入风险；NAS I/O 或本机资源异常仍可能失败。
- 预算为一次 GRAB + ARCTIC 串行全量处理，仅 GPU2，不使用其他用户 GPU 进程；完成即退出，不自动重试失败任务。
- schema / non-finite / OOM / 写入错误不得触发后续依赖任务；记录日志、producer manifest 和服务退出码。
- 新启动前 GPU2 须至少 20 GiB 空闲；输入、资产、NAS 挂载和目标根预检失败时不启动。

## 验证与回滚

- 启动前核对空目录清单、未改变的导出相关代码、源 index、资产和原 manifest；不将重新验证计数当作新导出数量。
- 启动后验证 user service 的 MainPID / cgroup 不属于当前会话工具，检查日志和 manifest 持续更新、failures 为空。
- 退出时保存 exit code / signal / service result；只有 producer `COMPLETED` 且正式 index / cache manifest 完整时才接受完成。
- 停止本次 unit 即可中断新运行；保留新运行日志、所有新旧 geometry 和 `.partial`。
  隔离目录仅在原 `.partial` 路径未被本次运行占用时才可原样改名返回；有新内容则不覆盖，保留两者并另行确认。
- 本计划不覆盖任何 FINAL plan；本次用户确认后按上述一次性路径例外和续跑执行。
