# ObjectInteractionCmv2 V1.9 指导：现有 validation split 二域评估

用户确认在当前 ARCTIC test split 缺失的前提下，先只做现有 validation split 的二域评估：GRAB 固定 stride `1`，ARCTIC 在 `5..10` 中按冻结的确定性 transition 规则直接选取 stride，并按实际 stride 分组报告。使用 V1.8.1 当前 `best.pt`（epoch 9）作为只读 checkpoint；训练继续运行。

不把 validation 解释为 held-out test，不修改 cache、GT、split、FK metadata 或 checkpoint，不使用仍在变化的 `latest.pt`。
