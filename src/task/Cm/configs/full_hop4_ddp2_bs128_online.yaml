_base_: src.task.Cm.config:Config

# Global batch size 128 via two DDP processes with per-process batch size 64.
name: cm_action_slot16_full_hop4_ddp2_bs128_online

meta:
  dense_checkpoint: outputs/train/correspondence_ptv3_v2_20260716_150803/checkpoints/best.pt

data:
  train_path: processed_data/generated/stage4/grab_cm_raw_stride3_hop4_4096_hand_root
  val_path: null
  val_split: 0.1
  batch_size: 64
  val_batch_size: 32
  num_workers: 0
  pin_memory: true
  persistent_workers: false
  prefetch_factor: null
  shuffle: true
  group_val_by_sequence: true
  active_only: true
  min_object_flow_norm: 0.0
  max_train_samples: null
  max_val_samples: null

train:
  output_dir: outputs/train/cm_action_slot16_full_hop4_ddp2_bs128_online
  seed: 42
  overfit_mode: false
  optimizer: adamw
  lr: 1.5e-4
  weight_decay: 1.0e-4
  scheduler: cosine
  warmup_ratio: 0.03
  warmup_steps: 0
  epochs: 20
  max_steps: null
  amp: false
  grad_clip_norm: 1.0
  log_every_steps: 20
  eval_every_epochs: 1
  save_every_epochs: 1
  metric_for_best: val/flow_residual_norm_mean
  lower_is_better: true
  distributed:
    enable: true
    backend: auto
    timeout_minutes: 60
    broadcast_buffers: false
    find_unused_parameters: false

wandb:
  enable: true
  project: ref2dex
  mode: online
  tags: [cm, slot-attention, slot16, grab-full, hop4, ddp2, gb128, online, group-held-out]
