_base_: src.task.Cm.config:Config

name: cm_sequence_one_sixth_dir_s4to12_scale100_bs512_lr6e4_online

meta:
  dense_checkpoint: /home/wbcd/workspace/oyx_ws/Ref2Dex/src/task/Cm/densetoken_ckpt/best.pt
  internal_point_flow_scale: 100.0

data:
  train_path: processed_data/generated/cm_sequence_cache_one_sixth
  val_path: null
  val_split: 0.1
  batch_size: 256
  val_batch_size: 128
  num_workers: 4
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4
  shuffle: true
  group_val_by_sequence: true
  active_only: true
  min_stride: 4
  max_stride: 12
  val_stride: 4
  val_strides: [4, 5, 6, 7, 8, 9, 10, 11, 12]
  max_train_samples: null
  max_val_samples: null

train:
  output_dir: outputs/train/cm_sequence_one_sixth_dir_s4to12_scale100_bs512_lr6e4_online
  seed: 42
  overfit_mode: false
  optimizer: adamw
  lr: 6.0e-4
  weight_decay: 1.0e-4
  scheduler: cosine
  warmup_ratio: 0.03
  warmup_steps: 0
  epochs: 20
  max_steps: null
  amp: false
  grad_clip_norm: 1.0
  log_every_steps: 20
  eval_every_epochs: 1
  save_every_epochs: 1
  metric_for_best: val/mean_stride_flow_mse
  lower_is_better: true
  distributed:
    enable: true
    backend: auto
    timeout_minutes: 60
    broadcast_buffers: false
    find_unused_parameters: false

wandb:
  enable: true
  project: ref2dex
  mode: online
  tags: [cm, sequence-cache, one-sixth-dir, stride4-12, scale100, ddp2, gb512, online]
