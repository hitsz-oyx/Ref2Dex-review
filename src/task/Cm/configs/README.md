# Cm 配置目录

配置只按生命周期分为两个目录：

- `active/`：当前允许新实验、评估或迁移使用的配置；
- `archive/`：仅用于复现历史实验的冻结配置。

不再按 `finetune`、`ablation`、`evaluation` 等实现细节建立一级目录。实验性质写在配置元信息、实验日志和结果目录中。

当前顶层 YAML 暂时保留为兼容入口。迁移完成后，规范路径应指向 `active/` 或 `archive/`；旧路径不得静默改变科研语义。
