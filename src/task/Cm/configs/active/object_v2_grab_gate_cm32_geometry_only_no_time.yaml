_base_: src/task/Cm/configs/active/object_v2_grab_gate_cm64_warmup.yaml

name: cm_object_v2_grab_gate_cm32_geometry_only_no_time

meta:
  cm_dim: 32
  use_object_context: false
  use_time_condition: false
  use_slot_gate: true
  gate_warmup_enabled: true
  gate_warmup_full_epochs: 5
  gate_warmup_ramp_epochs: 5
  loss_slot_count_weight: 1.0e-3
  loss_slot_confidence_weight: 0.1
  loss_active_overlap_weight: 0.0

train:
  description: "Fresh GRAB object-only Cm: C=32, raw object geometry only, no time condition, Hard-Concrete gate loss with 5+5 epoch warm-up."

wandb:
  mode: offline
  tags: [cm, object-v2, grab-only, cm32, geometry-only-object, no-time, hard-gate, gate-warmup, fresh, frozen-densetoken]
