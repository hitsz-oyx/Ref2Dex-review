_base_: src/task/Cm/configs/active/object_v2_grab_additive_cm64_geometry_only_no_time_budget50.yaml

name: cm_object_v2_grab_additive_cm32_geometry_only_no_time_budget50

meta:
  # Strict C=32 additive capacity comparison: only Cm width changes from the
  # C=64 additive strict-budget configuration.
  cm_dim: 32

train:
  description: "GRAB object-only C=32 additive contribution strict budget match: global batch 64, 50 epochs, aggregate supervision, group sparsity."

wandb:
  tags: [cm, object-v2, grab-only, cm32, geometry-only-object, no-time, additive-contribution, group-sparsity, strict-budget50]
