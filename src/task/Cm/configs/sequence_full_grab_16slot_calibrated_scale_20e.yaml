_base_: sequence_full_grab_16slot_20e_fresh.yaml

name: cm_full_grab_16slot_calibrated_scale_20e

meta:
  # full_grab_v1 train-only statistics.  Geometry uses the rounded common
  # component-RMS normalization selected from the train split.
  flow_target_rms_m: 0.09268507747442897
  geometry_input_scale: 15.0
  hand_flow_input_scale: 17.71962521771656
  object_flow_target_scale: 10.78922332751884
  # Stage 4 is shared with earlier experiments and currently has no root-level
  # calibration metadata.  Keep the exact calibration self-contained here.
  require_flow_calibration: false

train:
  description: "Fresh full GRAB (1068 train seqs): 16 slots, no gate, train-distribution geometry/hand-flow/object-flow scales, physical time conditioning, 20 epochs, two-GPU DDP."

wandb:
  enable: true
  mode: online
  tags: [cm, full-grab, slot-16, no-gate, calibrated-scale, geometry-15, time-conditioned, ddp-2, fresh, 20e]
