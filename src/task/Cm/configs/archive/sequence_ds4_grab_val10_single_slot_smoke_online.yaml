_base_: src.task.Cm.src.config:Config

name: cm_sequence_ds4_grab_val10_single_slot_smoke_online

meta:
  dense_checkpoint: /home/wbcd/workspace/oyx_ws/Ref2Dex/src/task/Cm/densetoken_ckpt/best.pt
  internal_point_flow_scale: 100.0
  num_cm_tokens: 1
  loss_slot_count_weight: 0.0
  loss_slot_confidence_weight: 0.0

data:
  train_path: data/processed_data/stage4/data/GRAB/ds4_full
  val_path: null
  test_path: null
  val_split: 0.1
  active_only: true
  min_stride: 1
  max_stride: 10
  val_stride: 1
  val_strides: [1, 2, 3]
  group_val_by_sequence: true
  max_train_samples: 512
  max_val_samples: 128
  batch_size: 8
  val_batch_size: 8
  num_workers: 0
  pin_memory: true
  persistent_workers: false

train:
  output_dir: outputs/train/cm_sequence_ds4_grab_val10_single_slot_smoke_online
  device: cuda
  lr: 3.0e-4
  amp: false
  epochs: 1
  max_steps: 32
  log_every_steps: 10
  eval_every_epochs: 1
  save_every_steps: 100
  save_every_epochs: 1
  metric_for_best: val/mean_stride_epe_mm
  lower_is_better: true
  distributed:
    enable: false
    backend: auto
    timeout_minutes: 60
    broadcast_buffers: false
    find_unused_parameters: false

wandb:
  enable: true
  project: ref2dex
  mode: online
  tags: [cm, grab, ds4, smoke, single-slot, online]
