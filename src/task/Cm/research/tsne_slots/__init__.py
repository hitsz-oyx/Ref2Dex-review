"""Task-local t-SNE diagnostic experiment package.

The implementation remains in :mod:`run`; the package exists so that the
experiment can carry its own documentation, manifest, and ignored outputs.
"""
