# correspondence_ptv3_v2 V1 清理计划（final）

## 范围

- 删除 Task 内此前新增的 `components/`、`data/` 和 `registry/` 入口；
- 删除根 `components/ref2dex/correspondence_ptv3_v2` 兼容入口；
- 移除 correspondence 配置中的 Task Component 清单字段；
- 保留并直接使用根级 `data/`、`dataset/`、`third_party/` 以及 Task 内已有的校准和研究代码。

## 不变量与保护边界

- 不复制、移动或删除任何真实数据、cache、checkpoint、output 或运行进程；
- 不修改用户指导、历史实验结论、坐标系、GT、split 或数据 schema；
- correspondence 模型、Dataset、Runner 和训练配置的科学语义保持不变。

## 验证与回滚

- 检查 Task 内入口和活动引用均消失；
- 运行 correspondence 配置导入和相关测试；
- `git diff --check`；
- 回滚通过恢复本计划涉及的 Git 路径和旧入口，不触碰根级数据文件。
