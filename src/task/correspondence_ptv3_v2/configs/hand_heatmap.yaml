_base_: src/task/correspondence_ptv3_v2/configs/baseline.yaml

name: correspondence_ptv3_v2_hand_heatmap

meta:
  # B: object-conditioned hand heatmap multi-task supervision, while the
  # unified PTv3 input remains the original 11-dimensional local feature.
  hand_context_dim: 0
  loss_hand_contact_weight: 0.005

wandb:
  tags: [correspondence, ptv3, v2, random128, contact-aux, hand-heatmap, lambda005]
