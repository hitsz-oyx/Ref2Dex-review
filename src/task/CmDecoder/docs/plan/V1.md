# CmDecoder V1 清理计划（final）

## 范围

- 删除 Task 内此前新增的 `components/`、`data/` 和 `registry/` 入口；
- 删除根 `components/ref2dex/cmdecoder_pointflow` 兼容入口；
- 移除 CmDecoder 配置中的 Task Component 清单字段；
- 保留并直接使用根级 `data/`、`dataset/`、`third_party/`、`outputs/` 和历史 checkpoint。

## 不变量与保护边界

- 不复制、移动或删除任何真实数据、cache、checkpoint、output 或运行进程；
- 不修改用户指导、历史实验结论或数据 schema；
- Task 的 Dataset、Runner、模型和训练命令保持原有实现。

## 验证与回滚

- 检查 Task 内入口和活动引用均消失；
- 运行 CmDecoder 配置导入和相关测试；
- `git diff --check`；
- 回滚通过恢复本计划涉及的 Git 路径和旧入口，不触碰根级数据文件。
