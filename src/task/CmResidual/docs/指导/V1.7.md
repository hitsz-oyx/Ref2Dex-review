# CmResidual V1.7 用户指导

- status: `user-confirmed`
- source: 用户要求在具备训练资格的 corrected reference 上继续，其他训练参数由 Agent 决定。

## 目标

使用 V1.6 `training_eligible=true` corrected reference 启动 CmResidual 的正式 PPO 训练，并保留完整可复现运行
记录。正式训练从随机 residual policy 开始，不加载 V1.5 pilot checkpoint。

## 保持不变

- frozen DExplore/OI-Cm checkpoint、2005D observation、18D action、reward、mimic、residual scale、坐标系和
  `[44,410]` source interval 不改变。
- `reference.allowIneligibleFor` 必须为空；训练只能通过 v2 manifest 的准入检查。
- 旧 v1 reference、pilot/zero-residual outputs、source/cache 和 shared runtime 不覆盖。
