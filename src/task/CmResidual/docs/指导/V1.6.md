# CmResidual V1.6 用户指导

- status: `user-confirmed`
- source: 用户要求“使用具备训练资格的 corrected reference，其余你自己定”。

## 目标

将已有 fixed-wrist、constrained-q6 corrected reference 完成 D1 训练准入审计，使用源 tensor 中有明确列合同的
raw object/contact labels 和同一 URDF 的可复现手物表面距离门，生成新的 `training_eligible=true` reference。
随后正式训练只允许读取这个新 artifact；旧 `reference_tracking_v1` 保留为历史诊断证据。

## 保持不变

- 旧 reference、source tensor、geometry cache、checkpoint、共享 `src/base/`、其他 Task 和已有运行不覆盖、不删除。
- 坐标系、367 帧 `[44,410]`、固定 `X_HB`、q6/mimic 合同、速度/加速度硬门和 DExplore teacher source 不改变。
- 不把几何候选 mask 当作 contact；只使用 source tensor 文档明确的 `205:206` object contact flag。
- 训练预算、seed、GPU、奖励和 action schema 由独立正式训练 plan 冻结，不在本版本中扩大。
