# CmResidual V1.18d：V1.14a teacher-to-actor pilot

- status: `FINAL`
- work_version: `V1.18d`（隔离探索，不移动主线 V1.21 指针）
- parent: [V1.18](V1.18.md)、[V1.18c](V1.18c.md)
- branch: `ai/cmresidual/v118c-v114a-integration`
- impact: L2；GPU smoke/pilot 为 L3
- approval: 用户确认先探索既定的 candidate scoring → actor distillation 路线并要求继续

## Goal / Change

允许 V1.14a adapter 在显式 pin 的 Inspire-compatible checkpoint 上进入有界 Stage B。runner 必须校验 actor/Cmv2
checkpoint SHA 和 Cmv2 `active_groups` 中的 `grab/inspire_f1`；rollout 记录 activation、valid fraction、
teacher weight、`||teacher-mu||` 与 predicted cost improvement。先做最小 smoke，再从固定 Phase-A checkpoint
运行小预算 pilot；不得直接进入 50M/C 阶段。

## Protected

保持 1442-D observation、18-D action、K=8 candidates、teacher cost、PPO reward/termination、PhysX、reference、
action authority 与 transition-only buffer 不变。Cm frozen 且不进入 actor/critic/reward；实际 rollout 仍执行 PPO
sampled action，teacher 只通过 stop-gradient `lambda_cm=0.10` actor loss 生效。

## Verification / Stop conditions

执行 checkpoint provenance、Hydra/launcher、teacher buffer/loss、定向 pytest、`verify.py --changed` 和 diff check；
GPU smoke 必须 finite 且 teacher diagnostics 非退化。checkpoint 不含 Inspire、teacher 永远无效/恒等、loss 非有限或
吞吐不可运行时停止，不启动 pilot。pilot 只比较 matched `lambda=0/0.10` 工程与抓取指标，不外推多轨迹效果。

## Rollback

revert V1.18d runner/diagnostic/test/docs 增量，恢复 V1.18c smoke-only 门禁；保留数据、checkpoint 和 run outputs。
