# CmResidual V1.17 执行计划（最终版）

- status: `final`（2026-09-18 经用户确认；容量 smoke 后用户明确改为单轨迹并授权测量稳定 env 数）
- modification_version: `V1.17`
- 对应指导：[指导/V1.17.md](../指导/V1.17.md)
- task_mode: `change`，完成后进入 `run-only/operation`
- operation_category: `code`、`experiment`、`operation`、`documentation`
- change_level: `L3`
- approval: `user-approved`；用户确认停止当前长训后按此范围训练 DExplore，并允许按需安装缺失运行依赖。

## 目标与范围

在不改动外部 DExplore checkout 的前提下，新增 CmResidual Task-local 启动器，运行 DExplore 原始
Inspire state-based PPO teacher 的受控容量 smoke。该运行是 CmResidual 的外部基线，不改变 CmResidual
模型、reference、Cmv2、reward 或 buffer 合同。

固定输入与运行合同：

- external source：`/home2/wyy/oyx_ws/dexplore`，启动时严格记录 Git commit 和 clean 状态；
- GRAB data：`data/processed_data/inspire_rl_object_dexplore/s1_airplane_lift/interaction_hand_inspire.pt`。
  sequence ID 与 CmResidual 已使用的 `s1_airplane_lift` 对齐；DExplore 输入的手 18-DOF 和物体 pose 来自已有
  `inspire.pth` 在 Isaac Gym 中的实际 rollout，本次待训练的 policy 仍从零初始化；
- GPU：physical GPU5；子进程设 `CUDA_VISIBLE_DEVICES=5`，将外部代码固定的逻辑 `cuda:0` 映射到该物理卡；
- runtime：独立环境 `/home2/wyy/oyx_ws/.runtime_envs/dexplore_v117`，固定 `rl-games==1.1.4`；不改动共享
  `graspenv` 的 `rl-games==1.6.5`；
- 容量 probe：单轨迹重复采样，horizon 64、minibatch 256、1 PPO iteration、seed 42、从零初始化；从 64 env
  开始，按成功档位倍增，直到 OOM、allocator/illegal-memory access、非有限张量或 GPU5 空闲余量不足，再以最后
  通过档位为稳定 env 数；
- output：新的 `outputs/Dexplore/<run_id>/`，含 `config.json`、`run_manifest.json`、`train.log`、
  仅含 660 条序列符号链接的 `motion_input/`、DExplore checkpoint/TensorBoard 子目录；
- 不使用 Horovod。若 4 env 无法运行，停止并记录，不能将多个旧 PPO rollout 混合为同一 on-policy batch；
  后续正式训练以更多独立 iteration 累积总环境步。

## 实现

1. 在 `src/task/CmResidual/tools/run_dexplore_grab_teacher.py` 新增 Task-local smoke launcher：验证外部源、
   资产、源根 660 条数据及指定 `s1_airplane_lift` 单样本 `(T,598)` float32 finite 合同、`rl-games==1.1.4`
   运行时与 GPU5 空闲门；将
   DExplore YAML 和 CLI override 写入 config snapshot，并在启动前创建 run manifest。
2. launcher 从外部 DExplore 根运行 `dexplore/run.py`，只通过 CLI 传入 `--motion_file`、`--output_path`、
   GPU5、4 env、horizon 64、minibatch 256、max iterations 1 和 seed 42。使用实际支持的 `--output_path`，
   不使用遗留脚本中的 `--output`。`inspire_rl_object_dexplore` 内的 `_logs/` 不是 motion；启动器在新运行
   目录只物化 `s1_airplane_lift` 的符号链接视图，避免外部 loader 将日志目录当作样本。
3. 子进程完成后更新 manifest：成功时记录 checkpoint/TensorBoard 路径与 exit code；失败时记录失败原因和
   保留 train log。任何输出目录都必须是新目录，禁止覆盖。
4. 更新本 Task 的版本指针、指导、计划、README 和 activity。新建测试只验证启动器的纯输入/参数合同；实际
   smoke 负责 GPU、Isaac Gym 和 PPO 证据。

## 验证与停止

- 静态：`py_compile`、启动器 `--help`、数据/外部 source/GPU preflight 的 dry run、定向测试、`git diff --check`。
- 运行：每个 env 档位必须生成独立 manifest、config、train log、TensorBoard event 和非空 checkpoint；记录
  退出码、last epoch/frame（可得时）、GPU peak memory 与工程结论。
- OOM、allocator/illegal-memory access、非有限张量、缺失 checkpoint/event 或退出码非零均为
  `FAILED` / `INVALID_IMPLEMENTATION`，不进入正式训练。
- 回滚：删除 V1.17 launcher、测试、指导/计划和本版本 activity/README/版本指针的差异；输出、DExplore
  checkout、GRAB data 和 V1.16.5 停止产物保留。

## 正式单卡训练追加合同（最终）

用户于 2026-09-18 授权 Agent 自主确定长训预算，并明确选择 2048 env。本次正式运行记录为 `V1.17.1`，该追加合同只启动一个从零初始化的
单轨迹 DExplore teacher 训练，不改变本计划的数据、坐标、奖励、物理、观测、动作或外部源码不变量：

- physical GPU5（`CUDA_VISIBLE_DEVICES=5`）、逻辑 `cuda:0`、single-rank 2048 env、horizon 64、minibatch 256、
  seed42、`s1_airplane_lift`；不使用 Horovod 或任何 capacity-smoke checkpoint。
- 外部实现的停止语义为 `epoch_num > max_epochs`，故设置 `max_iterations=152`，实际完成 epoch 1–153，预算为
  `153×2048×64=20,054,016` env-steps。每 38 epoch（约 `4,980,736` env-steps）保存中间 checkpoint，终态另保存 latest。
- 每个 checkpoint 门检查训练进程存活、日志中无 traceback/non-finite、TensorBoard event 更新且 checkpoint 非空；
  失败、OOM、非有限数值或缺失 checkpoint 立即停止并记录。训练 reward 仅作优化诊断，不能作为抓取成功或收敛结论；
  训练结束后才以固定 checkpoint 另行制定评估运行。

## V1.17.2 三卡 Horovod 追加合同（最终）

用户于 2026-09-18 明确要求停止上述单卡 DexPlore 运行，并在 physical GPU `0,1,3` 执行同步三卡 DexPlore 训练；该要求同时确认本追加合同。原 single-rank output 作为停止证据保留，不恢复其 checkpoint，也不覆盖其目录。

- 外部 DExplore 的真实多卡路径为 `--horovod`，不能用 Ref2Dex V1.16 的 `torch.distributed` launcher 替代。因此新建隔离运行时 `/home2/wyy/oyx_ws/.runtime_envs/dexplore_v117_hvd`：Python 3.8、PyTorch `2.0.1+cu118`、`rl-games==1.1.4`、Horovod `0.28.1`、Open MPI/NCCL；该版本替换了已实证无法编译 Horovod 的 `2.2.2+cu121`，不修改共享 `graspenv`、原 `dexplore_v117` 或外部 DExplore checkout。
- `CUDA_VISIBLE_DEVICES=0,1,3`，通过 `horovodrun -np 3 -H localhost:3` 启动；每个 Horovod rank 使用其 local rank 对应的逻辑 `cuda:0/1/2`。单 rank 的 `num_envs=2048`、horizon `64`、minibatch `256`、seed `42`、单轨迹 `s1_airplane_lift`、18-D action、1442-D observation、reward、物理和从零初始化不变；总 rollout env 为 `6144`，这是同步梯度训练而非三份独立 PPO。
- 先运行新输出目录中的 1-iteration 三卡 capacity/synchronization smoke：必须三 rank 成功初始化、parameter/stat synchronization 可见、每 rank log/exit code 正常、rank 0 checkpoint 与 TensorBoard event 非空、无 OOM/non-finite，才可启动正式训练。
- smoke 通过后正式训练以新的 `run_id` 从零开始，保持每 rank 2048 env 和原 `max_iterations=152`（外部停止语义下实际最多 153 epoch）；总目标为 `153×3×2048×64=60,162,048` environment steps，每 38 epoch由 rank 0 保存 checkpoint。失败、信号停止、Horovod collectives 错误或 checkpoint/event 缺失均停止并记录，不把 checkpoint 解释为抓取效果。
- Task-local launcher 可扩展为独立 multi-rank mode 与 output-local config/manifest；其测试必须覆盖 Horovod 命令、GPU 映射、每 rank/total env 的预算与不恢复 single-rank checkpoint。不得为此改写 DExplore 源码；若兼容运行时无法构建/导入 Horovod，记录 `INVALID_IMPLEMENTATION`，不以独立进程替代同步训练。

## V1.17.3 双卡 Horovod 追加合同（最终）

用户于 2026-09-18 明确将 V1.17.2 的三卡执行改为 physical GPU `0,3` 的双卡同步训练，并授权停止仅占用这两张卡、由 `graspenv` 启动的其他进程。该追加合同覆盖 V1.17.2 的 GPU/world-size 约定，其他研究与工程不变量不变：

- 使用 `CUDA_VISIBLE_DEVICES=0,3` 与 `horovodrun -np 2 -H localhost:2`；rank 0/1 映射逻辑 `cuda:0/1`，每 rank `num_envs=2048`、horizon 64、minibatch 256、seed42、`s1_airplane_lift`、从零初始化不变。总 rollout env 为 `4096`，仍为同步梯度训练而非两份独立 PPO。
- 先执行 1-iteration 双卡 capacity/synchronization smoke，目标 `2×2048×64=262,144` environment steps；必须两 rank 均初始化、collective/stat/parameter synchronization 无错误、rank 0 checkpoint 与 TensorBoard event 非空、无 OOM/non-finite，才可启动 formal。
- formal 保持每 rank 2048 env 和 `max_iterations=152`（实际最多 153 epoch）；总目标为 `153×2×2048×64=40,108,032` environment steps，rank 0 每 38 epoch 保存 checkpoint。不得恢复此前单卡 checkpoint，不得改变外部 DExplore、数据、观测、动作、奖励或物理合同。

## V1.17.4 rank-local Isaac Gym 默认设备 shim（最终）

双卡 smoke 已证实外部 DExplore 的多卡路径在 rank1 初始化时存在设备错配：`isaacgym.torch_utils.to_torch` 的默认参数硬编码为 `cuda:0`，而 rank1 PhysX 为 `cuda:1`。用户要求继续双卡运行，本追加合同授权最小的 Task-local launcher 修复：

- 新增 Ref2Dex bootstrap，在导入 DExplore 前以 `HOROVOD_LOCAL_RANK` 替换 Isaac Gym `to_torch` 的**未显式传入** `device` 默认值；显式 device 参数、DExplore 源码、Isaac Gym 安装文件、输入数据、任务/物理/奖励/观测/动作与每 rank 2048 env 合同均不得修改。
- Horovod rank、`CUDA_VISIBLE_DEVICES=0,3`、rank0/1 的逻辑 `cuda:0/1` 保持不变。bootstrap 只由 `--launcher horovod` 调用，单卡既有命令保持 `dexplore/run.py`。
- 重新使用新 run_id 执行 smoke；必须达到完整 PPO epoch、rank 0 checkpoint/TensorBoard 非空且无跨设备/OOM/non-finite，才可 formal。此前所有失败目录保留为 `INVALID_IMPLEMENTATION` 证据，绝不覆盖。
