# CmResidual V1.1 执行计划（最终版）

- status: `final`
- direction_approval: `user-approved`
- execution_approval: `architecture-and-code-user-approved`
- modification_version: `V1.1.0`
- 对应指导：[指导/V1.0.md](../指导/V1.0.md)
- 主任务模式：`governance/change`
- 操作类别：`governance`、`architecture`、`code`、`documentation`
- 影响等级：`L3`（vendor Task 所有权、公共 import、checkpoint/观测/动作合同）

## 1. 目标与边界

本版本将 CmResidual 的执行入口迁移为
`third_party/IsaacGymEnvs/isaacgymenvs/tasks/cm_residual/` Python package，并以 DExplore
Inspire teacher `inspire.pth` 作为冻结 base policy。残差策略与 Cm online/target 双模型在同一
Isaac Gym 环境中训练；本版本只实现可导入、可验证的接口和控制合同，不启动 PPO 长训，也不提交
checkpoint、cache、output 或外部 DExplore 工作树中的文件。

用户已明确确认：取消 vendor `logs*` 忽略、使用 `inspire.pth`、同步 DExplore observation、使用
DExplore 原生 18D action。由此覆盖 V1.0 中旧的 12D residual action 合同；V1.0 数据产物和旧配置
保留为历史，不被新入口静默重解释。

## 2. 锁定合同

- observation：`1442` 维，按 DExplore 两个时间偏移（`delta_t=1`、`delta_t=16`）拼接；缺失的
  reference/state 字段必须显式填零并在 manifest/日志中标记，禁止把未来 simulator state 当作 GT。
- action：`18` 维，前 6 维为 wrist，后 12 维为 Inspire finger native action。残差输出同样为
  18D，组合为 `clip(a_dexplore(obs) + delta, -1, 1)`，然后逐字节保持 DExplore
  `_action_to_pd_targets` 映射和 Inspire mimic 比例 `1.05/0.6/0.8`。
- base checkpoint：`inspire.pth`，加载前校验 observation/action 维度、网络 key 和可选 SHA256；参数
  全部 `requires_grad=False`。默认路径通过配置提供，不硬编码为某台机器的绝对路径。
- Cm：online encoder/adapter 参与 replay/supervised update；target 在每个 PPO block 开始冻结，
  仅由 EMA 或显式 sync 更新；actor 与 critic 都接收 Cm feature。
- scene/control：保持 DExplore Inspire URDF、DOF 顺序、30 Hz control（`dt=1/60`、`controlFrequencyInv=2`）、
  PD gains 和 collision filter。当前 Ref2Dex URDF 与 DExplore URDF SHA256 必须一致，否则 fail closed。

## 3. 文件变更

- 将旧 `tasks/cm_residual.py` 迁移为 package 内 `task.py`；`__init__.py` 重新导出
  `CmResidual` 及历史别名。
- 新增 `base_policy.py`（DExplore checkpoint loader、running mean/std、1442→18 contract）、
  `cm_adapter.py`（online/target 生命周期和特征接口）。
- 取消 vendor `.gitignore` 的 `logs*` 规则，使
  `tasks/cm_residual/docs/logs/architecture_log.md` 成为可审计记录；不追踪运行日志目录中的生成产物。
- 更新 vendor task/train 配置为 1442/18 和 DExplore checkpoint 参数，旧配置文件保留并显式标为 legacy。

## 4. 验证与回滚

验证命令：

```bash
python3 -m py_compile third_party/IsaacGymEnvs/isaacgymenvs/tasks/cm_residual/*.py
PYTHONPATH=third_party/IsaacGymEnvs python3 -m pytest -q src/task/CmResidual/tests/test_reference_contract.py
python3 .agents/skills/research-change-control/scripts/audit_diff.py --worktree --check-links
```

工程 smoke 只证明 package 可导入、checkpoint contract 和 action/observation 维度成立，不构成
训练效果结论。回滚入口为恢复旧 `tasks/cm_residual.py`、旧配置和 `.gitignore` 的单独提交；不删除
历史数据或用户 dirty diff。
