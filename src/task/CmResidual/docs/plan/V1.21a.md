# CmResidual V1.21a 执行计划（最终版）：冻结 Cmv2 的 Phase B 蒸馏

- status: `final`
- work_version: `V1.21`
- 对应指导谱系：[V1.21](../指导/V1.21.md)
- 适用计划：[V1.21](V1.21.md)
- task_mode: `run`
- change_level: `L2`
- approval: `user-approved`
- approval_basis: 用户于 2026-09-19 在 Phase A 完成后明确指示“继续后面的工作”。

## 目标与固定合同

以 Phase-A 的明确 checkpoint
`outputs/CmResidual/cmresidual_v121_phasea_refppo_gpu6_20260919/train/CmResidualGrabReferenceV118_a/nn/CmResidualGrabReferenceV118PPO_19-21-27-14.pth`
为唯一初始 checkpoint，运行 Phase B planner-to-actor distillation。

除 `cm_distill_coef` 从 `0` 改为 `0.10` 外，保持 Phase A 的 `s1_airplane_lift` 输入及 SHA、1442-D actor/critic、18-D residual、seed 42、GPU6、128 env、64-step window、奖励、termination、坐标/单位、action authority、transition-only buffer 和 PPO 配置不变。冻结 Cmv2 的 SHA、FP32 model config、K=8、all-active 16-env microbatch 与 object chunk 32 不变；Cm 只生成 rollout-time detached teacher，不能直接执行 action、进入 critic/reward 或更新参数。

## 预算、门禁与证据

运行 10 个实际 PPO epochs（81,920 frames）；launcher 将该实际预算映射为 vendored trainer 的 `max_epochs=9`。每个 run 使用新 output/run_id，manifest 固定 predecessor checkpoint 与 SHA、git commit、资源、输入和预算。

成功的工程门为：完成 10 epoch、无 OOM/NCCL/PhysX/traceback、finite checkpoint、checkpoint 可读、日志存在，且 Cm-on planner 在 rollout 中执行。若 non-finite、OOM、checkpoint 失败或 Cm teacher 运行错误，立即停止，不改变 K、microbatch、Cmv2、数据或动作合同。

训练终态写 Activity 与 experiment card。该有限单 seed 运行只能支持工程 runnability；不得据 reward、loss 或瞬时 lift 宣称抓取、收敛或 Cm utility。128/366 window、多卡、online planner 执行和 Cmv2 fine-tune 仍排除在外。

## 回滚

本补充不修改训练代码或已有输出；运行失败只保留独立 output。后续代码/文档回滚仅回退 V1.21a 的显式提交，不删除 checkpoint、buffer、data 或历史 outputs。
