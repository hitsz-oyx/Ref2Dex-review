# CmResidual V1.20b 执行计划（最终版）：DExplore vendor snapshot

- status: `final`
- work_version: `V1.20`
- 对应指导谱系：[指导/V1.20.md](../指导/V1.20.md)
- 适用计划：[V1.20](V1.20.md)、[V1.20a](V1.20a.md)
- task_mode: `governance` → `change`
- operation_category: `governance`、`code`、`documentation`
- change_level: `L3`
- approval: `user-approved`
- approval_basis: 用户于 2026-09-19 明确确认采用 `third_party/DExplore` 的固定 vendor snapshot，而非 submodule。

## 目标

将当前已验证可与 `dexplore_v117` 运行时配合的 DExplore 源码固定为仓库内
`third_party/DExplore/` squashed vendor snapshot，使 V1.20a DDP bootstrap 有可审计、repo-relative
且不受外部 checkout 未跟踪输出影响的源码入口。

## 范围与保护项

- 输入只取 `/home2/wyy/oyx_ws/dexplore` commit
  `c31f57f186409ce5f0de47ced2d347abffe45d06` 的 Git 已跟踪源码与资产；不复制其未跟踪 `outputs/`，
  也不复制该 commit 中的 LFS checkpoint pointers 或已 smudge 的 checkpoint 内容。
- snapshot 不携带嵌套 `.git`；保留上游 `LICENSE`、`NOTICE` 和全部 attribution。
- 新增 `third_party/DExplore/UPSTREAM.md`，记录 canonical upstream/fork URL、固定 commit、许可、
  导入方式、SHA256 和本地集成边界；仅新增 vendor-local `/outputs/` 忽略规则，防止误写运行产物。
- 将 V1.20a Task-local launcher 的默认 DExplore entrypoint 改为 `third_party/DExplore/dexplore/run.py`；
  保留 `--dexplore-run` 显式覆盖，不能静默选择外部 checkout。
- 不修改、移动、reset 或清理 `/home2/wyy/oyx_ws/dexplore`、其 `outputs/`、Python 环境、数据、
  checkpoint、Horovod 环境或任何历史运行。

## 验证

1. vendor 目录无 `.git`、无 `outputs/`、无 checkpoint/cache；`git diff --check` 通过。
2. `UPSTREAM.md` 的 commit、source SHA256 与 vendor `dexplore/run.py` SHA256 可复核；许可证文件存在。
3. DDP launcher dry-run 的 entrypoint 为 repo-local vendor 路径，且定向测试、语法检查和
   `python tools/verify.py --changed` 通过。
4. 本计划不启动 GPU smoke；vendor 接入只支持源码/provenance 工程合同，不产生科研结论。

## 回滚

回退本次 vendor 提交即可移除 `third_party/DExplore`、provenance、忽略规则和 launcher 默认路径；
外部 checkout 与所有 outputs 不受影响。
