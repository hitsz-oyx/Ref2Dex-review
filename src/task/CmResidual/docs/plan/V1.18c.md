# CmResidual V1.18c 计划：OICmv2 V1.14a 抓取接线

- status: `FINAL`
- work_version: `V1.18c`（隔离性能探索，不移动 V1.21 指针）
- guidance: [V1.18](../指导/V1.18.md)、[V1.18a](../指导/V1.18a.md)、[V1.18b](../指导/V1.18b.md)
- branch: `ai/cmresidual/v118c-v114a-integration`
- impact: L2；GPU smoke 为 L3
- approval: 用户确认训练结束后直接转入抓取任务，先做 checkpoint 单步推理和最小 rollout 接线，不继续消耗在 OIC 训练上

## Change / Protected

新增 opt-in V1.14a frozen adapter，使 V1.18 planner 对一个 state 只编码一次物体，并以 candidate axis 一次评价
K=8。刚体 airplane 使用单 part；输入转换到当前物体局部坐标。Inspire 表面固定为单手 2048 点，不伪造不存在的
第二只手。保持 1442-D observation、18-D action、candidate、teacher cost、reward、termination、PhysX、PPO 和旧
V1.3 默认路径不变。

checkpoint 固定为本地 GRAB/MANO 1-epoch `best.pt` 及 SHA。由于它未训练 Inspire 域，本轮只验证严格加载、finite
单步、候选共享计算和 rollout FPS；不据此判断抓取效果或 Cmv2 utility，也不启动新训练。

## Verification / Rollback

先做 CPU 合同测试与 GPU 单步延迟，再做最小 1-env/短 horizon rollout；记录旧 V1.3 与 V1.14a 的调用形状、延迟和
FPS。若 schema、坐标、finite 或运行接线失败即停止。回滚只 revert 本计划后的 adapter/config/runner/test 提交；
合并前两条已保留分支及所有 checkpoint、数据、cache、outputs 不变。
