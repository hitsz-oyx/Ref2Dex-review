# 研究工程规范

本项目以最小、正确、可验证的改动推进研究。首要目标是尽快获得可信的实验结论，而不是追求最大化的软件工程完整性。

## 默认语言

- 项目文档、`research_log.md`、实验记录和 Git 提交信息默认使用中文。
- 代码标识符、第三方接口名、标准缩写和必须保持兼容的 schema 字段可以保留英文。
- 用户明确指定其他语言时，以用户要求为准。

## 工作流程

研究和实验任务遵循：

1. 先阅读相关代码，理解现有执行路径。
2. 写一个简短研究计划，说明假设、最小实验和预期可观测结果。
3. 尽快实现并运行实验，不长时间停留在方案讨论。
4. 优先产出可运行代码、定量指标、日志、图表或直接观察。
5. 获得足够证据后记录结论，并决定保留、撤回或进行下一个实验。

优先采用：

```text
检查 -> 假设 -> 修改 -> 运行 -> 观察 -> 记录
```

避免：

```text
检查 -> 猜测 -> 设计 -> 写长文档 -> 重设计 -> 泛化 -> 实现
```

## 研究优先级

优先考虑：

- 可运行实验；
- 最小原型；
- 定量结果；
- 直接对比；
- 能回答具体问题的消融；
- 相关行为的可视化；
- 简洁、可复查的实验记录。

如果修改约 20 行代码并运行一次实验就能检验假设，应先这样做，不要先搭建通用框架。

## 最小正确实现

- 只做验证当前假设或实现当前需求所需的最小改动。
- 遵循现有目录结构和代码约定，不重构无关代码。
- 不为一次性实验引入多余抽象、包装层、兼容层或配置系统。
- 没有明确兼容要求时，不为旧实验逻辑增加 fallback；过时逻辑优先删除。
- 在简单实验实现和面向假想未来需求的泛化实现之间，选择前者。

## 必要的可信度检查

只进行会影响实验结论可信度的检查，例如：

- tensor shape 和维度；
- NaN / Inf；
- 坐标系、单位和尺度；
- train/val/test 划分；
- 数据对应关系和时间顺序；
- loss 量级；
- 数据泄漏；
- 参数或梯度是否真实变化；
- baseline 是否能复现；
- 指标是否真的反映研究假设。

除非用户明确要求，或它直接影响结论可信度，否则不要把研究任务扩大成全面 QA、CI、部署加固、异常输入防御或覆盖率工程。

判断标准是：

> 这个问题会不会让实验结论变错？

如果不会，通常应优先运行下一个信息量更高的实验。

## 失败处理

- 开发阶段优先 fail-fast。
- 不用宽泛异常捕获隐藏意外错误。
- 不静默替换 fallback 值，不自动重试无法解释的失败。
- 调查会影响当前实验的失败；无关且不阻塞的问题只需简短记录。
- 理论风险或附近 bug 不会自动成为当前任务范围。

## 范围纪律和完成条件

每个任务应：

1. 明确目标或研究假设；
2. 检查相关代码；
3. 定义最小信息性实验或 patch；
4. 实现并运行；
5. 检查数值或行为结果；
6. 记录学到的内容；
7. 达到验收标准后停止。

一旦证据足以回答问题，不继续增加推测性鲁棒性、无关测试或架构重设计。

## research_log.md

研究型任务必须维护项目根目录的 `research_log.md`，默认使用中文，内容简洁、事实化。

每个有意义的实验大致使用：

```markdown
## 实验：<简短名称>

**假设**
预期什么，为什么。

**观察到的失败 / 现象**
什么行为促成了这个实验。

**诊断**
当前对原因的最佳解释。

**改动**
修改了什么代码、配置、数据或超参数。

**结果**
定量指标或具体行为变化。

**决策**
保留 / 撤回 / 结论不充分。

**下一步**
仅在存在明显下一实验时填写。
```

只记录有研究意义的实验，不记录每条命令。优先写具体数字，使其他研究者能理解测试内容、原因、改动、结果和决策。

## 递归项目文档

项目采用由总到分的递归文档结构：

- 根级入口：`docs/项目总览.md`，说明整个工程的目标、主要目录、共享流程和各子任务索引。
- task 级入口：每个任务在自己的 `src/task/<Task>/docs/项目说明.md` 中维护该任务的目标、数据、模型、运行方式、实验结论、当前状态和下一步。
- 更复杂的任务可以在自己的 `docs/` 下继续拆分设计、数据、实验等文档，并由该 task 的 `项目说明.md` 建立索引。

AI 开始修改某个 task 前，应先阅读根级 `docs/项目总览.md` 和该 task 的 `docs/项目说明.md`（如果存在），再阅读被它们引用且与当前工作相关的文档。

代码、数据 schema、运行方式或已确认结论发生变化时，应同步修改最靠近该事实的文档：

- 全局事实更新根级文档；
- task 局部事实更新 task 文档；
- 有研究意义的实验同时更新 `research_log.md`。

文档应帮助后续 AI 自上而下理解项目，不写成长篇流水账，也不复制可以通过代码直接获得的大段细节。

当前 InteractionDynamics 任务入口为：

`src/task/InteractionDynamics/docs/项目说明.md`

## Git 提交

- 用户要求提交时，提交信息默认使用中文，简洁说明研究或实现结果。
- 提交前检查 `git status` 和 diff，只纳入当前任务相关文件。
- 不提交数据缓存、训练输出、临时文件或大模型权重，除非用户明确要求。
- 不修改或覆盖用户已有的无关改动。
