# 一、通用研究工程规范

## 1. 默认语言

- 项目文档、研究日志、实验记录和 Git 提交信息默认使用中文。
- 代码标识符、第三方接口名、标准缩写、命令、配置字段和必须保持兼容的 schema 字段可以保留英文。
- 用户明确指定其他语言时，以用户要求为准。

---

## 2. 基本工作流程

研究和实验任务默认遵循：

1. 先阅读相关代码，理解现有执行路径。
2. 写一个简短研究计划，说明：
   - 当前假设；
   - 最小实验；
   - 预期可观测结果。
3. 尽快实现并运行实验，不长时间停留在文档规划和方案讨论。
4. 优先产出：
   - 可运行代码；
   - 定量指标；
   - 日志；
   - 图表；
   - 可视化；
   - 直接行为观察。
5. 获得足够证据后记录结论，并决定：
   - 保留；
   - 撤回；
   - 修改假设；
   - 进行下一个实验。

优先采用：

```text
检查 -> 假设 -> 修改 -> 运行 -> 观察 -> 记录
```

避免：

```text
检查 -> 猜测 -> 设计 -> 写长文档 -> 重设计 -> 泛化 -> 实现
```

除非用户明确要求设计方案，否则不要长时间停留在 planning 阶段。

### 2.1 用户级目标与中间 Gate

研究任务必须区分：

- 用户级研究目标；
- 指导文档中定义的完整实验路线；
- 中间研究假设；
- implementation gate；
- sanity check。

其中：

> 中间 gate 的通过表示可以继续推进，不表示整个任务完成。

例如当前研究路线为：

```text
实现
-> forward sanity
-> 真实数据短训
-> baseline
-> 正式训练
-> evaluation
-> intervention
-> 形成结论
```

则：

```text
forward 成功
```

只表示：

```text
可以进入真实数据训练
```

而不是：

```text
当前任务完成
```

同理，以下结果默认都属于中间 gate，而不是用户级任务完成条件：

- import 成功；
- tensor shape 正确；
- forward 无报错；
- NaN / Inf 检查通过；
- 单 batch 可训练；
- synthetic data 实验成功；
- 单样本 overfit；
- 数十步或数百步短训 loss 下降；
- 单序列实验成功。

除非用户明确只要求验证上述问题，否则在这些 gate 通过后应继续推进。

如果：

- 下一步已经明确；
- 不需要用户提供新的信息；
- 当前环境具备运行条件；
- 下一步仍直接服务于当前用户级目标；

则默认继续执行下一步，不因为一个局部实验成功而停止。

研究任务的默认行为应是：

```text
快速通过必要 gate
-> 尽快进入真实实验
-> 连续推进主要实验链路
-> 获得能够回答用户级问题的证据
-> 再停止
```

连续推进过程中发现新的潜在问题时，不应自动为每个问题创建新的诊断实验。

只有当该问题：

- 阻塞下一阶段；
- 会使当前结果无法解释；
- 或可能使核心结论错误；

才优先调查。

否则记录后继续主实验链路。

Gate 只用于解除当前实验真实存在的可信度风险，不应为了遵循流程而人为增加 Gate。

如果现有证据已经足以进入下一阶段，应直接进入下一阶段，不需要额外设计一个 smoke test、短训或诊断来"证明可以继续"。

### 2.2 指导文档执行原则

如果用户已经提供研究指导文档、实验方案或明确的执行路线，应将其中与当前用户请求直接相关的连续实验链路视为当前研究任务的主要 scope。

用户当前明确请求的范围优先于指导文档。
指导文档用于决定当前目标内部如何连续推进，不自动扩大用户明确指定的任务边界。

指导文档不是待用户逐项触发的 TODO 菜单。

指导文档中的步骤还应区分：

- 当前阶段明确要求完成的主实验链路；
- 条件触发的备选实验；
- 后续工作、未来方向和可选优化。

只有当前阶段的主实验链路默认连续执行。

对于"如果失败再做""必要时再做""后续考虑""未来可以尝试"等条件性或未来性内容，
只有触发条件真实成立，或它对回答当前用户级问题确有必要时才执行；
不要仅因为它出现在指导文档中就自动展开。

如果指导文档定义：

```text
A
-> B
-> C
-> D
```

并且 A 完成后 B 的执行条件已经满足，则应继续执行 B；
B 完成后继续执行 C。

不要默认采用：

```text
完成 A
-> 写结论
-> 停止
-> 等用户再次要求 B
```

而应优先采用：

```text
完成 A
-> 检查结果
-> 若结果允许，立即进入 B
-> C
-> D
-> 在足够回答研究问题后统一形成结论
```

只有以下情况可以提前偏离或停止指导路线：

1. 当前假设已经被明确证伪，导致后续实验失去意义；
2. 后续步骤缺少必要数据、环境、权限或依赖；
3. 后续步骤涉及用户未授权的高成本资源消耗、付费服务、不可逆操作或明显超出当前研究任务正常规模的计算，需要用户决策；
4. 实际结果发现指导文档中的前提不成立，需要先修改研究假设；
5. 已经获得足够证据回答用户提出的核心研究问题。

如果只是某个 sanity check、短训或局部 gate 通过，不属于上述停止条件。

正常的训练、仿真、evaluation、benchmark 和数据处理，只要属于当前指导路线且环境资源已经可用，不应仅因为运行时间更长而停止或请求确认。

---

## 3. 研究优先级

优先考虑：

1. 可运行实验；
2. 最小原型；
3. 定量结果；
4. 直接对比；
5. 能回答具体问题的消融；
6. 相关行为的可视化；
7. 简洁、可复查的实验记录；
8. 工程泛化和抽象。

如果修改约 20 行代码并运行一次实验就能快速检验假设，应先这样做，不要先搭建通用框架。

但“最小实验”用于快速获得方向反馈，不是默认任务终点。

如果最小实验通过，并且下一步实验路径已经明确，应立即继续推进后续实验，而不是因为当前局部假设已经得到验证就停止。

如果一个问题可以通过一次小实验直接验证，不应仅通过代码阅读和理论推测得出结论；验证后若仍未回答用户级研究问题，应继续进入下一阶段。

---

## 4. 最小正确实现

- 只做验证当前假设或实现当前需求所需的最小改动。
- 遵循现有目录结构、接口和代码约定。
- 不重构与当前任务无关的代码。
- 不为一次性实验引入多余的：
  - 抽象层；
  - wrapper；
  - compatibility layer；
  - factory；
  - plugin architecture；
  - 配置系统。
- 没有明确兼容要求时，不为旧实验逻辑增加额外 fallback。
- 明确过时且阻碍当前实验的逻辑可以删除。
- 在简单实验实现和面向假想未来需求的泛化实现之间，默认选择前者。
- 只有出现真实、重复的需求后，再考虑抽象和泛化。

---

## 5. 必要的可信度检查

只进行会影响实验结论可信度的检查，例如：

- tensor shape 和维度；
- dtype；
- NaN / Inf；
- 坐标系；
- 单位和尺度；
- train / val / test 划分；
- 数据对应关系；
- 时间顺序；
- loss 量级；
- 数据泄漏；
- 参数是否真实变化；
- 梯度是否真实传播；
- baseline 是否能够复现；
- 指标是否真的反映研究假设；
- 对比实验是否只改变目标变量。

判断标准是：

> 这个问题会不会让当前实验结论变错？

如果不会，通常不应让它阻塞当前研究。

除非用户明确要求，或者它直接影响实验结论可信度，否则不要把研究任务扩大成：

- 全面 QA；
- CI；
- 大规模单元测试；
- 测试覆盖率工程；
- hash / checksum 系统；
- 全面格式化；
- 部署加固；
- 极端异常输入防御；
- 与当前实验无关的性能优化。

---

## 6. 失败处理

开发和研究阶段默认采用 fail-fast。

- 不使用宽泛异常捕获隐藏意外错误。
- 不静默替换 fallback 值。
- 不吞掉 exception。
- 不对原因未知的失败进行无限自动重试。
- 遇到错误时优先暴露真实 stack trace 和失败状态。
- 调查会影响当前实验结论的失败。
- 无关且不阻塞当前任务的问题只需简短记录。
- 理论风险或附近代码中的潜在 bug 不自动成为当前任务范围。

发现额外问题时首先判断：

> 它是否影响当前实验结果？

如果不影响，记录即可，不立即扩大任务范围。

---

## 7. 范围纪律和完成条件

每个任务应：

1. 明确用户级目标以及当前研究假设；
2. 检查相关代码；
3. 选择当前最直接的信息性实现或实验；必要时先做最小 Gate；
4. 实现并运行；
5. 检查数值或行为结果；
6. 根据结果决定：
   - 进入下一实验；
   - 修改假设；
   - 撤回当前方案；
   - 或形成最终结论；
7. 连续推进直到达到用户级目标或主要研究验收标准；
8. 最后统一记录关键结论。

### 中间 Gate 与完成条件

以下两类标准必须区分：

```text
中间 Gate
```

用于判断：

```text
能否继续下一阶段
```

例如：

- forward 正确；
- 数据读取正确；
- loss 可以下降；
- gradient 正常；
- 单样本可以 overfit；
- baseline 可以运行。

而：

```text
任务完成条件
```

用于判断：

```text
当前用户级研究问题是否已经得到回答
```

例如用户要求判断一个新 representation 是否有效，则通常至少需要：

```text
实现
-> 真实数据训练
-> baseline
-> evaluation
-> 必要 intervention / ablation
-> 得出支持或反对该表示的证据
```

不能因为其中某一个中间 Gate 通过就认为整个任务完成。

默认执行路径：

```text
阅读
  ↓
提出假设
  ↓
最小修改
  ↓
运行
  ↓
检查结果
  ↓
Gate 通过则进入下一阶段
  ↓
形成能够回答用户级问题的结论
```

一旦证据足以回答当前用户级问题，不继续增加：

- 推测性鲁棒性；
- 无关测试；
- 新抽象；
- 通用框架；
- 架构重设计；
- 没有明确需求的优化。

---

## 8. 长时间运行任务与异步等待

对于训练、仿真、编译、数据处理、模型下载、视频渲染、benchmark 以及其他长时间运行任务，禁止通过高频 polling 反复唤醒模型。

这里的 polling 指：

> Agent 为了确认后台任务是否完成，反复调用工具查询状态，而不是让工具自身长时间等待。

核心原则：

> 让工具等待，不要让模型每隔几秒重新推理一次来确认任务是否结束。

这既减少无意义的模型调用，也避免长上下文在每次轮询中被重复处理造成大量 token 消耗。

### 8.1 空 `write_stdin` polling

当 `write_stdin` 不发送任何实际内容，只用于查询后台进程状态时：

- `yield_time_ms` MUST >= `180000`；
- 不需要中间输出时优先使用 `300000`；
- 禁止每隔数秒或几十秒执行一次空 `write_stdin`。

推荐：

```text
启动任务
-> 等待 180~300 s
-> 获得输出或状态变化
-> 再决定下一步
```

避免：

```text
启动任务
-> 10 s
-> poll
-> 10 s
-> poll
-> 10 s
-> poll
-> ...
```

### 8.2 `functions.wait`

对于长时间运行的异步任务：

```text
functions.wait
```

必须使用：

```text
yield_time_ms >= 180000
```

不需要中间状态时，优先让 wait 长时间阻塞并自然返回，而不是主动进行短间隔轮询。

### 8.3 嵌套工具等待

如果外层 `functions.exec` 中存在内部长等待工具：

```text
functions.exec
└── nested tool wait
```

则外层 `@exec yield_time_ms` 必须至少比最长的内部等待多：

```text
30000 ms
```

即：

```text
outer_exec_yield_time
>=
longest_nested_wait + 30000
```

例如：

```text
nested wait: 180000 ms
outer exec:  >= 210000 ms
```

避免出现：

```text
外层 exec 先 yield
-> Agent 被重新唤醒
-> 内层工具实际上仍然在等待
-> Agent 再次进入等待
-> 产生无意义模型调用
```

### 8.4 非空 `write_stdin`

上述长等待规则只适用于纯状态 polling。

如果 `write_stdin` 正在发送实际输入，例如：

```text
"y\n"
"\n"
Ctrl-C
交互式命令输入
```

应立即发送，不要人为等待 180~300 秒。

因此需要区分：

```text
write_stdin("")
```

表示：

```text
查询状态
-> 长等待
```

而：

```text
write_stdin("实际输入")
```

表示：

```text
与进程交互
-> 立即发送
```

### 8.5 长任务默认行为

对于预期运行超过几十秒的命令：

- 尽量一次启动后长时间等待；
- 不频繁检查 stdout；
- 不因为暂时没有输出而主动高频 poll；
- 不为了展示"任务仍在运行"而反复调用模型；
- 只有中间输出会实际影响下一步研究决策时，才提前检查；
- 如果任务预计需要数分钟，应按照分钟级而不是秒级进行状态检查。

尤其适用于：

```text
训练
仿真
Isaac Sim
MuJoCo
CUDA 编译
数据预处理
模型下载
benchmark
视频渲染
大规模测试
```

---

## 9. Agent 与子任务使用原则

不要为了形式上的 Agent 化而拆分大量子任务。

简单问题优先由当前 Agent 直接完成。

只有在以下情况才考虑并行或独立子任务：

- 多个调查方向彼此独立；
- 多个模块可以并行阅读；
- 大型仓库需要明确按模块拆分；
- 独立实验可以并行运行；
- 最终需要单独 review 一个已经完成的实现。

避免：

```text
收到任务
-> planner
-> researcher
-> coder
-> tester
-> reviewer
-> 每个 Agent 再继续创建子 Agent
```

如果主 Agent 自己可以在较少步骤内完成任务，不要创建额外子 Agent。

子任务数量应以提高研究效率为目标，而不是最大化并行度。

---

# 二、本仓库规范

## 10. 研究目录与 log.md

需要开展研究时，在对应 Task 下新建研究目录，并把研究 log 文档放在该目录下，诊断脚本也写到里面。固定结构：

```text
src/task/<Task>/research/
├── log.md               # 该 Task 全部实验的唯一研究日志
└── <实验名>/            # 诊断脚本按实验组织
    ├── diag_xxx.py
    └── ...
```

规则：

- `log.md` 与 `research/<实验名>/` 下的诊断脚本需要纳入版本管理，不 ignore。
- 每个实验用独立子目录放置诊断脚本；脚本产生的大型详细产物不放入该目录。
- `log.md` 记录该 Task 的所有实验；如果 `output/research/` 中出现了重要数据，应把关键结论摘要记录进 `log.md`，方便以后查看。

实验产物统一放到仓库根目录 `output/` 下：

```text
output/
├── exp/                # 训练实验输出
└── research/           # 分析诊断产物（与 research/<实验名>/ 脚本对应）
```

`output/exp/` 与 `output/research/` 都被 gitignore 忽略；详细产物不提交，只有摘要级结论写入 `log.md`。

`log.md` 默认使用中文，内容简洁、事实化。

每个有意义的实验大致使用：

```markdown
## 实验：<简短名称>

**假设**
预期什么，为什么。

**观察到的失败 / 现象**
什么行为促成了这个实验。

**诊断**
当前对原因的最佳解释。

**改动**
修改了什么代码、配置、数据或超参数。

**结果**
定量指标或具体行为变化。

**决策**
保留 / 撤回 / 结论不充分。

**下一步**
仅在存在明显下一实验时填写。
```

只记录有研究意义的实验，不记录每条命令。

不要求每个中间 gate 通过后立即形成独立实验记录。

以下内容通常不需要单独形成一个研究实验条目：

- import / dependency 修复；
- shape check；
- 单次 forward；
- NaN / Inf 检查；
- 为正式实验服务的临时诊断；
- 很短的 smoke training；
- 单纯确认代码路径能够运行。

多个属于同一研究假设的连续实验可以先连续执行，完成一个有意义的研究阶段后统一记录。

例如：

```text
forward sanity
-> 必要的短训验证
-> 足够规模的真实训练
-> baseline / evaluation / intervention
```

中间训练规模应按“是否还需要解除实验可信度风险”决定，不默认逐级执行固定的 10 / 100 / 1000 step gate。
如果已经有充分依据进入正式训练，应直接进入正式训练。

如果它们都用于验证同一个核心假设，可以统一记录成一个实验阶段，而不是为了写 log 人为拆成多个研究任务。

`log.md` 的记录时机不应阻塞实验继续推进。

记录优先包含：

- 具体实验变量；
- 具体数字；
- baseline；
- 对比结果；
- 可观察行为变化；
- 最终决策。

目标是让其他研究者能够快速回答：

```text
为什么做？
改了什么？
发生了什么？
是否有效？
是否保留？
```

不要把 `log.md` 写成操作流水账。

---

## 11. 递归项目文档

本仓库采用由总到分的递归文档结构。

### 11.1 根级入口

整个工程的入口文档：

```text
docs/项目总览.md
```

用于说明：

- 整体研究目标；
- 主要目录；
- 共享数据和流程；
- 主要 Task；
- 各子任务文档索引；
- 全局已经确认的事实。

### 11.2 Task 级入口

每个任务在：

```text
src/task/<Task>/docs/项目说明.md
```

维护自己的任务说明。

至少应覆盖：

- 任务目标；
- 数据；
- 模型；
- 主要代码入口；
- 运行方式；
- 已确认实验结论；
- 当前状态；
- 下一步。

复杂 Task 可以继续在自己的：

```text
docs/
```

目录中拆分：

```text
设计
数据
实验
实现说明
其他专题文档
```

并由该 Task 的：

```text
docs/项目说明.md
```

建立索引。

---

## 12. 开始修改 Task 前的阅读顺序

AI 开始修改某个 Task 前，应按照以下顺序理解项目：

```text
docs/项目总览.md
        ↓
src/task/<Task>/docs/项目说明.md
        ↓
其中引用且与当前任务直接相关的文档
        ↓
相关代码
```

不存在的文档可以跳过。

不要为了开始一个局部实验而遍历整个 `docs/` 或整个仓库。

只读取当前研究问题所需的文档和代码。

文档的作用是帮助快速定位执行路径，而不是替代实际代码阅读。

---

## 13. 文档同步原则

代码、数据 schema、运行方式或已经确认的研究结论发生变化时，应同步修改最靠近该事实的文档。

规则：

```text
全局事实
-> docs/项目总览.md

Task 局部事实
-> src/task/<Task>/docs/项目说明.md

具体设计细节
-> 对应 Task/docs/ 下专题文档

有研究意义的实验
-> src/task/<Task>/research/log.md
```

不要在多个文件中复制维护同一份详细信息。

文档应帮助后续 AI 自上而下理解项目：

```text
项目
-> Task
-> 专题
-> 代码
```

不要写成长篇流水账。

可以直接通过代码快速获得的实现细节，不需要大段复制到文档。

---

## 14. InteractionDynamics

当前 InteractionDynamics 任务入口：

```text
src/task/InteractionDynamics/docs/项目说明.md
```

涉及 InteractionDynamics 的工作，默认首先阅读：

```text
docs/项目总览.md
src/task/InteractionDynamics/docs/项目说明.md
```

然后根据文档索引进入相关代码和专题文档。

如果 InteractionDynamics 的以下事实发生确认后的变化：

- 数据格式；
- 数据预处理流程；
- 模型结构；
- loss；
- 训练方式；
- 运行命令；
- evaluation；
- pipeline；
- 已确认实验结论；
- 当前状态；

应同步修改：

```text
src/task/InteractionDynamics/docs/项目说明.md
```

有研究意义的实验同时记录到：

```text
src/task/InteractionDynamics/research/log.md
```

---

## 15. 本仓库 Git 规范

用户要求提交时：

1. 提交前检查：

```bash
git status
git diff
```

2. 只纳入当前任务相关文件。
3. 不修改、覆盖、reset 或 revert 用户已有的无关改动。
4. 默认不提交：

```text
数据缓存
训练输出
临时文件
日志目录
checkpoint
大模型权重
自动生成的大型结果文件
```

除非用户明确要求。

5. Git commit message 默认使用中文。
6. Commit message 应简洁描述实际研究或实现结果，而不是泛泛描述操作。

例如优先：

```text
修正接触点时间对齐并验证 InteractionDynamics loss
```

而不是：

```text
修改代码
```

---

# 三、执行原则摘要

遇到研究任务时，默认按照：

```text
读文档
-> 读相关代码
-> 明确用户级研究目标
-> 提出简短假设
-> 最小修改 / 最小实验
-> 运行
-> 检查结果
-> 必要 Gate 通过
-> 立即进入下一阶段
-> 必要 sanity check
-> 真实训练 / baseline / evaluation / intervention
-> 获得能够回答核心问题的证据
-> 统一更新 research/log.md
-> 更新最靠近事实的项目文档
-> 停止
```

始终优先：

```text
可信实验结果
```

而不是：

```text
更复杂的工程结构
```

始终优先：

```text
实际运行验证
```

而不是：

```text
长时间理论猜测
```

始终优先：

```text
最小信息性实验作为快速反馈手段
```

而不是：

```text
把最小实验本身当作任务终点
```

默认：

```text
最小实验
-> 获得反馈
-> 如果方向成立，立即扩大到足够回答研究问题的实验
```

而不是：

```text
最小实验
-> 成功
-> 停止
```

不要把研究流程理解为“尽可能少做工作”，而应理解为“用尽可能少的无效工作，连续推进到足以回答用户级研究问题”。
