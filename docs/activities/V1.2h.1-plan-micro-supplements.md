# ACT-V1.2H.1 — plan 点数字微补充规则

- timestamp: 2026-09-20T22:00:00+08:00
- activity_id: ACT-20260920-V12H1-PLAN-MICRO-SUPPLEMENTS
- work_version: V1.2h
- base_commit: `787f047`
- branch: `ai/cmresidual/v121e-snapshot-restore`
- scope: repository governance
- change_level: L3
- approval: user-approved
- approval_basis: 用户提出小任务纠偏复用现有分支并采用 `V1.21e.1` plan，明确批准修改规范

## Changes and protected semantics

AGENTS、change-control、experiment-workflow、directory Skill 与治理测试同步增加
`V<n><letter>.<k>.md` plan 微补充；固定适用边界、整数排序、审批、读取顺序和分支复用理由。
work_version、指导命名、稳定分支、研究语义及既有历史 plan/outputs 均不改变。

## Verification and rollback

- `python3 -m pytest -q tests/governance`：`10 passed`（3 条既有 NumPy alias warning）；
- `quick_validate.py`：research-change-control、research-experiment-workflow、directory-and-artifacts
  三个 Skill 均为 `Skill is valid!`；
- `python3 tools/verify.py --changed` 与 `git diff --cached --check`：通过；裸 `python` 在本机指向
  Python 2，未进入 verifier，故不作为验证结果。

回滚为 revert 本治理提交；不会删除或重解释既有 plan、Activity 与运行产物。
