# ACT-V1.2H — 计划定稿规则

timestamp: 2026-09-19T17:00:00+08:00
activity_id: ACT-20260919-V12H-PLAN-FINALIZATION-RULE
work_version: V1.2h
base_commit: b7f0bae
branch: ai/governance/plan-finalization
scope: repository governance
change_level: L3
approval: user-approved
approval_basis: 用户确认草案可原地修改，定稿计划必须以字母后缀追加。

## Goal

区分协商中的 plan 草案与已定稿 plan 的修改方式，既避免草案产生无意义的文件，又保护定稿合同的追溯性。

## Changes

- 非 `FINAL` plan 可在原文件原地修订。
- `FINAL` plan 的实施合同变更必须新建字母后缀 plan；有触发指导时复用其字母，否则使用下一个未使用字母。
- AGENTS、change-control Skill 和治理测试同步该规则；根 work_version 更新为 V1.2h。

## Protected semantics

- 不改变指导谱系排序、同文件冲突确认、研究语义或现有历史计划。
- 不触碰用户 CmResidual 分支及其未跟踪 `V1.20a.md`。

## Verification

- `conda run -n graspenv python -m pytest -q tests/governance tests/test_run_manifest.py`：`15 passed`，
  有 3 条既有 NumPy alias FutureWarning。
- `conda run -n graspenv env VERIFY_BASE=b7f0bae python tools/verify.py --changed`：`VERIFY PASS`。
- `git diff --cached --check`：通过。

## Rollback

反转本次独立提交即可恢复原规则，不影响研究产物或用户工作树。

## References

- [V1.2h governance plan](../plan/V1.2h.md)
