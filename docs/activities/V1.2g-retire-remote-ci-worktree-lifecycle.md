# ACT-V1.2G — 停止远端 CI，保留本地门禁与工作树生命周期

timestamp: 2026-09-19T16:30:00+08:00
activity_id: ACT-20260919-V12G-RETIRE-REMOTE-CI-WORKTREE-LIFECYCLE
work_version: V1.2g
base_commit: b216a58
branch: ai/governance/push-ci-worktree-lifecycle
scope: repository governance
change_level: L3
approval: user-approved
approval_basis: 用户决定放弃 CI 方向，要求删除此前规范中的 CI 测试要求，同时保留工作树规范。

## Goal

停止维护远端 GitHub Actions CI，保留本机变更验证和顺序工作树复用/清理保护。

## Changes

- 删除 GitHub Actions verify workflow。
- 移除现行 AGENTS、change-control Skill 与测试目录规范中的 CI 运行和结果合同。
- 保留 `tools/verify.py --changed`、治理测试和 worktree 生命周期规则。
- 撤回尚未提交的 V1.2f CI 闭环及只为远端 CI 失败准备的 NumPy 2 兼容改动。

## Protected semantics

- 未修改研究代码、Task 测试、数据、cache、checkpoint、outputs、worktree 或分支。
- V1.2e 及更早的历史文档保持为审计事实，不构成当前 CI 规则。

## Verification

- 现行 AGENTS、change-control Skill、`tests/README.md` 与 `.github/` 的术语审计：无 CI、GitHub
  Actions、push 结果或 HEAD SHA 合同残留；已合并 V1.2e 及更早的历史记录不在审计范围内。
- `conda run -n graspenv python -m pytest -q tests/governance tests/test_run_manifest.py`：`14 passed`，
  有 3 条既有 NumPy alias FutureWarning。
- `conda run -n graspenv env VERIFY_BASE=b216a58 python tools/verify.py --changed`：`VERIFY PASS`。
- `git diff --cached --check`：通过。

## Rollback

反转本次独立提交即可恢复远端 workflow 与此前现行表述；不影响工作树或研究产物。

## References

- [V1.2g governance plan](../plan/V1.2g.md)
- [V1.2e historical CI selection activity](V1.2e-hermetic-ci-test-selection.md)
