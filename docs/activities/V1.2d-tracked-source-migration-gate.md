# ACT-V1.2D — work_version 迁移门禁的 tracked-source 边界

timestamp: 2026-09-19T13:00:00+08:00
activity_id: ACT-20260919-V12D-TRACKED-SOURCE-MIGRATION-GATE
work_version: V1.2d
base_commit: e8ff1e9
branch: ai/governance/fix-output-scan
scope: repository governance
change_level: L3
approval: user-approved
approval_basis: 用户确认仅修正迁移门禁范围，不修改历史运行产物。

## Goal

让 work_version 迁移门禁仅检查 Git 跟踪的活动 Task 源码与配置，排除不提交的历史
`research/**/output/**` 运行产物。

## Changes

- 测试以 `git ls-files src/task` 枚举候选文件，并跳过 `output` 路径组成部分。
- 保持 Python/YAML/JSON/TOML 的活动源码与配置扫描；历史 output 中的旧字段不迁移、不删除。
- 根 work_version 前移至 V1.2d，并增加计划与 Activity 追溯入口。

## Protected semantics

- 未修改任何历史 output、run manifest、数据、cache、checkpoint 或 Task 研究实现。
- 未改变 `work_version` 对 Git 跟踪活动源码/配置的唯一字段要求。

## Verification

- `conda run -n graspenv python -m pytest -q tests/test_run_manifest.py tests/governance`：
  `12 passed`；有 3 条既有 NumPy alias FutureWarning，无测试失败。
- `conda run -n graspenv env VERIFY_BASE=origin/oyx python tools/verify.py --changed`：
  `VERIFY PASS`，覆盖自稳定远端起点的完整治理与 CmResidual 合并差异。
- `git diff --cached --check`：通过。后续合回主工作树后还须在其现有历史 outputs 条件下复验。

## Rollback

反转本次独立治理提交即可恢复 V1.2c 的测试枚举；不影响实际产物。

## References

- [V1.2d governance plan](../plan/V1.2d.md)
- [Directory and artifacts Skill](../../.agents/skills/directory-and-artifacts/SKILL.md)
