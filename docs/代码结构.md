# Ref2Dex 代码结构

## 主数据流

```text
GRAB / ARCTIC raw data
  -> 数据集目录内解析和 Stage 2 生成
  -> 公共 Stage 2 sequence-hand pkl
  -> 公共 Stage 3 sequence-hand npz
  -> epoch 级 512 物体点采样和随机增强
  -> correspondence_ptv3
```

不生成或读取独立 Stage 1 中间数据。

## 目录

```text
Ref2Dex/
├── process/
│   ├── GRAB/                    # 数据集特定的 Stage 2 / Stage 4 入口
│   ├── ARCTIC/
│   ├── ContactPose/
│   └── common/                  # 跨数据集的 Stage 2 / 3 / 4 公共实现
├── data/                        # 机器本地数据；整体被 Git 忽略
│   ├── raw_data/                # 按数据集来源存原始下载与数据集专属资源
│   │   ├── GRAB/
│   │   │   └── objects/
│   │   ├── ARCTIC/
│   │   └── ContactPose/
│   ├── processed_data/          # 所有派生数据；不再使用 generated/ 层级
│   │   ├── stage2/
│   │   │   └── <variant>/
│   │   ├── stage3/
│   │   │   └── <variant>/
│   │   ├── stage4/
│   │   │   ├── data/            # 同一版本的完整数据；按来源组织子目录
│   │   │   │   ├── GRAB/        # GRAB 源数据生成的 stage4 样本
│   │   │   │   │   ├── s1/
│   │   │   │   │   ├── s2/
│   │   │   │   │   └── ...
│   │   │   │   └── meta.json    # schema、生成条件和统计元数据
│   │   │   │   ├── ARCTIC/      # ARCTIC 源数据生成的 stage4 样本
│   │   │   │   │   ├── s1/
│   │   │   │   │   └── ...
│   │   │   │   └── meta.json    # schema、生成条件和统计元数据
│   │   │   └── splits/          # 划分策略；不复制样本
│   │   │       └── sequence_split_v1/
│   │   │           ├── train.txt
│   │   │           ├── val.txt
│   │   │           ├── test.txt
│   │   │           └── split.json
│   │   └── stage5/
│   └── shared/                  # 跨数据集共用的本地资源
│       └── mano/
├── render/
├── src/
├── tools/
└── outputs/                     # 训练运行输出；整体被 Git 忽略
    └── <task>/
        └── <task>_<timestamp>/
            ├── config.json
            ├── metadata.json
            ├── metrics.jsonl
            ├── train.log
            ├── checkpoints/
            └── eval/ or test/   # 仅实际写评估产物时创建
```

## 数据目录约定与跨机器同步

- `data/raw_data/<dataset>/`：原始下载数据及只属于该数据集的资源；例如 GRAB
  物体网格位于 `data/raw_data/GRAB/objects/`。
- `data/shared/`：跨数据集共用资源；MANO 位于 `data/shared/mano/`。不再使用
  顶层 `assets/` 作为数据资源目录。
- `data/processed_data/stageN/`：按处理 stage 管理派生数据。`generated/` 已合并
  到 `processed_data/`，不应再创建该层级。
- Stage 4 的规范入口是 `data/processed_data/stage4/data/`。这里应最终包含完整
  数据；做小样本、不同留出策略或复现实验时，只更换 `splits/` 中的 split 文件，
  不复制或移动 `data/` 内的 `.npz`。
- split 文本每行是相对 `stage4/data/` 的样本路径；`split.json` 记录划分依据、
  随机种子、数据集来源和比例。

`data/` 与 `outputs/` 都不会提交到 Git。跨机器同步时，先用 Git 同步代码和本文档，
再用 `rsync -aH` 同步完整的 `data/`；只在需要复现某次结果时同步对应的
`outputs/<task>/<task>_<timestamp>/` 运行目录。

## 数据处理职责

`process/GRAB/raw.py` 和 `process/ARCTIC/raw.py` 只负责数据集特定逻辑：

- 读取原始文件；
- 统一米制单位和坐标定义；
- 重建 MANO 手部几何；
- 固定采样 4096 个物体表面点；
- 构造供公共 Stage 2 writer 使用的内存字段。

`process/GRAB/stage2_optimize.py` 和 `process/ARCTIC/stage2_optimize.py` 是用户入口。它们将
左右手拆成独立样本，并调用 `process/common/stage2.py`：

- 按 5cm hand-object 最近距离过滤帧；
- 生成公共字段和质量诊断；
- 写入 `data/processed_data/stage2/<variant>/`。

`process/common/stage3_corr.py` 不包含数据集分支。它负责：

- 将世界坐标转换到 hand-root frame（可选 object frame）；
- 保留 4096 个物体点；
- 计算 5cm object candidate mask 与 hand-to-full-object 最短距离；
- 输出仅供 correspondence_ptv3_v2 使用的最小 Stage 3 v2 schema；
- 默认写入 `data/processed_data/stage3/<variant>_v2/`。

当前训练集加载器 `src/task/correspondence_ptv3_v2/dataset.py` 负责运行时逻辑：

- 用 frame 和 epoch 生成确定性随机种子；
- 从候选池采样 512 个物体点；
- 候选不足时 padding；
- 对输入物体施加共享 SE(3) pose perturbation，手和 GT 均保持 clean；
- 构造 random128、contact-aux 与 dense hand heatmap 三条监督流。

## 模型代码

```text
src/task/correspondence_ptv3/
├── config.py
├── dataset.py
├── sampling.py
├── model.py
├── runner.py
├── train.py
├── eval.py
└── README.md
```

`model.py` 定义 PTV3 backbone、object-hand cross interaction 和当前单一
canonical correspondence head。`runner.py` 计算 loss 和指标；
`src/base/checkpoint.py` 管理原子 checkpoint。

## 可视化

- `render/stage2_visualize.py`：检查公共 Stage 2 几何、帧过滤和坐标系。
- `render/stage3_visualize.py`：显示 4096 点池、5cm 候选、实际采样 512 点、
  clean/noisy hand 以及 clean/input KNN。
- `tools/stage3_npz_to_ply.py`：将 Stage 3 帧导出为标准 binary PLY。

Open3D 使用：

```bash
export DISPLAY=localhost:10.0
```

具体数据字段以 [架构.md](./架构.md) 为准，处理命令见
[`process/README.md`](../process/README.md)。
