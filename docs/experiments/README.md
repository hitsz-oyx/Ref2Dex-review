# Ref2Dex Experiments

这里索引跨 Task 的正式科学证据。每张 experiment card 记录 hypothesis、protocol/configuration、runs、evidence、
conclusion 和 limitations；一个 card 可以关联多个 run。

当前没有需要写在根作用域的 experiment card。Task 专属实验写入相应 `src/task/<Task>/docs/experiments/`。

历史 experiment log（若某一作用域存在）保留只读审计价值，不批量迁移。
