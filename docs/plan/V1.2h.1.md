# V1.2h.1 治理微补充：已执行 plan 的点数字纠偏谱系

- status: `FINAL`
- work_version: `V1.2h`
- parent_plan: [V1.2h](V1.2h.md)
- impact_level: `L3`
- branch_reuse_reason: 用户明确要求小任务纠偏时可复用现有任务分支，不为本次规范修订新建分支
- approval: 用户明确提出 `V1.21e.1` 形式并批准修改规范

## 改变与保持

允许已执行的字母后缀 plan 在同一分支、同一研究问题和保护边界内，以
`V<n><letter>.<k>.md` 记录有界纠偏或探索重试；数字从 1 连续递增并按整数排序。微补充仍需
用户批准、完整保护项/风险/验证/回滚，不推进 work_version，也不自动授权正式运行。

指导命名仍只允许无后缀基线和单个 `a`–`z` 字母；独立研究问题、扩大保护边界或无界工作仍使用
下一字母 plan。`oyx` 稳定分支规则、L2/L3 审批、用户修改保护及输出不入 Git 均保持不变。

## 风险、验证与回滚

- 风险：点数字被当作任意追加通道；以同分支/同问题/同保护边界和有界范围限制。
- 风险：`.10` 被排在 `.2` 前；明确要求按整数排序。
- 风险：plan 与指导命名混淆；明确禁止点数字指导。
- 验证：governance pytest、Skill validator、`python tools/verify.py --changed`、链接与 diff 检查。
- 回滚：revert 本治理提交，恢复仅字母 plan 补充规则，不影响既有研究产物。
